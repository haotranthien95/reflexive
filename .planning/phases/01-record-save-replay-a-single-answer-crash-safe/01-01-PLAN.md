---
phase: 01-record-save-replay-a-single-answer-crash-safe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/db/database_helper.dart
  - lib/models/session.dart
  - lib/models/question_answer.dart
  - lib/services/recording_service.dart
  - lib/services/audio_player_service.dart
  - lib/utils/audio_paths.dart
  - lib/data/questions.dart
  - lib/state/practice_state.dart
  - lib/screens/practice_screen.dart
  - lib/screens/history_screen.dart
  - lib/screens/session_detail_screen.dart
  - android/app/src/main/AndroidManifest.xml
  - ios/Runner/Info.plist
  - test/models/session_test.dart
  - test/models/question_answer_test.dart
  - test/db/database_helper_test.dart
  - test/state/practice_state_test.dart
autonomous: true
requirements: [LOOP-03, LOOP-04, LOOP-05, LOOP-06, PERSIST-01, PERSIST-02, HIST-01, HIST-02, HIST-03, HIST-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Recording begins automatically the instant PracticeScreen opens, with no button tap required (D-01, LOOP-03)"
    - "Recording auto-stops at exactly 60 seconds if the user has not tapped Stop (LOOP-04, D-09)"
    - "A large, always-visible Stop button ends the current recording early and finalizes it before the 60s cap (LOOP-05)"
    - "The just-recorded answer plays back automatically the instant recording stops, with no user action needed (LOOP-06, D-10)"
    - "After auto-replay finishes, the screen resets to a freshly-picked hardcoded question and immediately starts recording again (D-03)"
    - "When the 60s auto-stop timer and a manual Stop tap occur at effectively the same moment, the first stop signal to fire wins and the second is a no-op (UI-SPEC unresolved overflow item — planner assumption, no distinct UI treatment)"
    - "A session row and its one question_answers row are written to SQLite inside a single db.transaction(), only after the audio file is fully finalized on disk — never before, and never as two independently-crashable writes (D-08, PERSIST-01)"
    - "A single answered question produces exactly one question_answers row plus one audio file, written immediately after capture, never buffered until later (PERSIST-01)"
    - "Two recordings made back-to-back each produce their own distinct session row — never merged, never overwritten (PERSIST-01/PERSIST-02 adjacency)"
    - "Sessions are keyed by an autoincrement primary key, so two sessions created within the same second remain two separately-listed history entries (HIST-01 adjacency)"
    - "History lists sessions ordered most-recent-first (id DESC) (HIST-01 ordering)"
    - "History shows the empty-state heading \"No recordings yet\" and body \"Finish your first answer and it'll show up here.\" when zero sessions exist (HIST-01 empty, UI-SPEC Copywriting Contract)"
    - "Opening a session in History shows its question_answers rows scoped strictly to that session_id (WHERE session_id = ?), in insertion order (id ASC) (HIST-02 adjacency + ordering)"
    - "No session with zero question_answers rows can ever appear in History — the session row and its one answer row are written together, in the same transaction, only after finalization (HIST-02 empty)"
    - "Tapping a question row in session detail plays that row's own recording via AudioPlayerService, independent of whether auto-replay already played it once (HIST-03)"
    - "Session detail screen title renders as \"Session — {date, time}\" (e.g. \"Session — Aug 7, 2:45 PM\") (UI-SPEC Copywriting Contract)"
    - "Audio file paths are stored relative to the app documents directory in the DB, and resolved to an absolute path only at read/play time — never stored as an absolute path (ARCHITECTURE.md Anti-Pattern 3)"
    - statement: "A brief system mic-permission dialog may appear before the very first recording starts, with no blank/frozen screen while awaiting the callback"
      verification: backstop
  artifacts:
    - path: "pubspec.yaml"
      provides: "Flutter project manifest declaring record, audioplayers, sqflite, path_provider, path dependencies and sqflite_common_ffi dev dependency"
    - path: "lib/main.dart"
      provides: "App entrypoint — runApp(MaterialApp(home: PracticeScreen()))"
    - path: "lib/db/database_helper.dart"
      provides: "DatabaseHelper class — sessions + question_answers schema, insertAnsweredSession, listSessions, listAnswersForSession"
      exports: ["DatabaseHelper"]
    - path: "lib/models/session.dart"
      provides: "Session model — id, createdAt, fromMap/toMap"
      exports: ["Session"]
    - path: "lib/models/question_answer.dart"
      provides: "QuestionAnswer model — id, sessionId, questionText, audioPath, createdAt, fromMap/toMap"
      exports: ["QuestionAnswer"]
    - path: "lib/services/recording_service.dart"
      provides: "RecordingService — start/stop wrapping package:record's AudioRecorder, 60s auto-stop, first-stop-wins guard"
      exports: ["RecordingService"]
    - path: "lib/services/audio_player_service.dart"
      provides: "AudioPlayerService — play(absolutePath)/stop() wrapping package:audioplayers's AudioPlayer"
      exports: ["AudioPlayerService"]
    - path: "lib/utils/audio_paths.dart"
      provides: "ensureRecordingsDir() and toAbsolutePath(relativePath) shared by RecordingService (write) and playback call sites (read)"
    - path: "lib/data/questions.dart"
      provides: "kQuestions — hardcoded rotating list of ~5 practice questions (D-02)"
    - path: "lib/state/practice_state.dart"
      provides: "PracticeState (ChangeNotifier) — PracticePhase enum, startNewQuestion(), stopRecording(), the full record/save/replay/reset loop"
      exports: ["PracticeState", "PracticePhase"]
    - path: "lib/screens/practice_screen.dart"
      provides: "PracticeScreen — auto-starts recording on open, Stop button, replay indicator, History nav icon"
    - path: "lib/screens/history_screen.dart"
      provides: "HistoryScreen — lists sessions most-recent-first, empty state, tap-to-open detail"
    - path: "lib/screens/session_detail_screen.dart"
      provides: "SessionDetailScreen — lists a session's question_answers, tap-to-replay"
  key_links:
    - from: "lib/state/practice_state.dart"
      to: "lib/services/recording_service.dart"
      via: "PracticeState.startNewQuestion() calls RecordingService.start(absolutePath); PracticeState.stopRecording() calls RecordingService.stop()"
      pattern: "recordingService\\.(start|stop)"
    - from: "lib/state/practice_state.dart"
      to: "lib/db/database_helper.dart"
      via: "After RecordingService.stop() returns a non-null finalized path, PracticeState calls DatabaseHelper.insertAnsweredSession(...) before any replay"
      pattern: "databaseHelper\\.insertAnsweredSession"
    - from: "lib/state/practice_state.dart"
      to: "lib/services/audio_player_service.dart"
      via: "After the DB write commits, PracticeState calls AudioPlayerService.play(absolutePath) for auto-replay (D-10)"
      pattern: "audioPlayerService\\.play"
    - from: "lib/screens/history_screen.dart"
      to: "lib/db/database_helper.dart"
      via: "HistoryScreen.build uses FutureBuilder(future: databaseHelper.listSessions())"
      pattern: "listSessions"
    - from: "lib/screens/session_detail_screen.dart"
      to: "lib/db/database_helper.dart"
      via: "SessionDetailScreen(sessionId) uses FutureBuilder(future: databaseHelper.listAnswersForSession(sessionId))"
      pattern: "listAnswersForSession"
  prohibitions:
    - "Recorded audio content or its file path must never be transmitted off-device by any analytics/crash-reporting/telemetry SDK or logging call — recordings stay strictly local per the project's explicit local-only architecture (no firebase_storage, no cloud sync)."
    - "The app must never silently delete, overwrite, or auto-prune a previously saved session or recording without an explicit user-initiated action in this phase — history is append-only; no background pruning job or storage-saving auto-delete."
    - "Copy shown around a stop/cutoff moment (manual Stop tap or the 60s auto-stop) must remain neutral, never mocking or shaming the user for not finishing their answer in time — consistent with a personal drill tool, not a punitive one."
---

<objective>
Establish the Flutter project itself and prove the phase's single highest-risk capability end-to-end: a question auto-records, is saved crash-safely to local storage the instant it finalizes, auto-replays, and appears (with its recording, replayable) in an Exercise History list — surviving a force-kill of the app.

Purpose: This is the Walking Skeleton for the entire EnglishReflex app (Phase 1 of a brand-new project). Every later phase (timed multi-question loop, Firestore question bank, JSON import) builds on the exact schema, service boundaries, and write-ordering established here without restructuring them.

Output: A runnable Flutter app (`flutter run`) with a working record -> save -> replay -> reset loop on the Practice screen and a working History list + session detail with tap-to-replay, backed by a crash-safe SQLite schema and local audio files.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-CONTEXT.md
@.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold the Flutter project and wire the record -> save -> replay -> reset loop end-to-end</name>
  <precondition>Flutter SDK is installed and `flutter --version` succeeds (a prerequisite for this greenfield scaffold; not something a prior task creates)</precondition>
  <reversibility rating="one-way">D-05 locks the `sessions` + `question_answers` two-table schema now (each Phase 1 recording is a session with exactly one question_answers row) so Phase 2 only adds rows, never migrates. This decision was already made by the user during discuss-phase (CONTEXT.md D-05) — recorded here for the schema-creation task, no re-confirmation checkpoint needed.</reversibility>
  <read_first>
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-CONTEXT.md
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-PATTERNS.md
    .planning/research/ARCHITECTURE.md
    .claude/CLAUDE.md
  </read_first>
  <files>pubspec.yaml, lib/main.dart, lib/db/database_helper.dart, lib/models/session.dart, lib/models/question_answer.dart, lib/services/recording_service.dart, lib/services/audio_player_service.dart, lib/utils/audio_paths.dart, lib/data/questions.dart, lib/state/practice_state.dart, lib/screens/practice_screen.dart, test/models/session_test.dart, test/models/question_answer_test.dart</files>
  <action>
    Run `flutter create --project-name englishreflex --org com.englishreflex .` in the repo root to scaffold the standard Flutter project (this generates `pubspec.yaml`, `lib/main.dart`, `android/`, `ios/`, `test/widget_test.dart`, etc. into the existing `.claude`/`.planning`/`docs` repo — do not overwrite `.claude/`, `.planning/`, `docs/`, `prompt.md`). Delete the generated default counter-app `test/widget_test.dart` (it references the stock counter widget which this task replaces).

    In `pubspec.yaml`, add dependencies: `record: ^7.1.1`, `audioplayers: ^6.8.1`, `sqflite: ^2.4.3`, `path_provider: ^2.1.6`, `path: ^1.9.0` (required by sqflite's `join()` path-building pattern, not yet listed in CLAUDE.md's stack table — add it there too as a one-line "tiny Dart-team-maintained path-joining utility, required by sqflite's documented usage pattern" entry). Add `sqflite_common_ffi: ^2.3.0` under `dev_dependencies` (enables `flutter test` to exercise `DatabaseHelper` against a real in-memory SQLite engine without a device/emulator, per the sqflite_common_ffi package's documented `sqfliteFfiInit()` + `databaseFactoryFfi` pattern). Run `flutter pub get`.

    Create `lib/db/database_helper.dart` with class `DatabaseHelper`. On first `Future<Database> get database` access, resolve the DB file path via `path_provider`'s `getApplicationDocumentsDirectory()` joined with `englishreflex.db` (via `package:path/path.dart`'s `join`), then `openDatabase(path, version: 1, onCreate: _onCreate)`. `_onCreate` executes exactly these two `CREATE TABLE` statements (verbatim, per PATTERNS.md): `sessions(id INTEGER PRIMARY KEY AUTOINCREMENT, created_at TEXT NOT NULL)` and `question_answers(id INTEGER PRIMARY KEY AUTOINCREMENT, session_id INTEGER NOT NULL REFERENCES sessions(id), question_text TEXT NOT NULL, audio_path TEXT NOT NULL, created_at TEXT NOT NULL)`. Add method `Future<int> insertAnsweredSession({required String questionText, required String audioRelativePath})` that opens `db.transaction((txn) async { ... })` and, inside that single transaction, inserts one `sessions` row (`created_at`: `DateTime.now().toIso8601String()`) via `txn.insert('sessions', {...})`, then inserts one `question_answers` row referencing the returned session id via `txn.insert('question_answers', {...})` using the typed `insert()` convenience method (never a raw, string-concatenated SQL statement) — return the session id. This method is the crash-safety-critical path (D-08, PERSIST-01): it must only ever be called AFTER the audio file is confirmed finalized on disk, never before.

    Create `lib/models/session.dart` (`Session` class: `int? id`, `DateTime createdAt`; `Session.fromMap(Map<String, Object?> map)`; `Map<String, Object?> toMap()` omitting `id` when null) and `lib/models/question_answer.dart` (`QuestionAnswer` class: `int? id`, `int sessionId`, `String questionText`, `String audioPath`, `DateTime createdAt`; same `fromMap`/`toMap` shape).

    Create `lib/utils/audio_paths.dart` with two top-level functions: `Future<Directory> ensureRecordingsDir()` (resolves `<appDocumentsDir>/recordings`, creates it with `create(recursive: true)` if missing, returns the `Directory`) and `Future<String> toAbsolutePath(String relativePath)` (joins `getApplicationDocumentsDirectory().path` with `relativePath` via `package:path`'s `join`). Both `RecordingService` (write path) and playback call sites (read path) use these — never construct or store an absolute path directly in the DB (ARCHITECTURE.md Anti-Pattern 3: iOS sandbox container paths can change across app updates).

    Create `lib/services/recording_service.dart` with class `RecordingService` wrapping `package:record/record.dart`'s `AudioRecorder`. Method `Future<bool> hasPermission()` delegates to the recorder's `hasPermission()`. Method `Future<void> start(String absoluteFilePath, {void Function()? onAutoStop})` calls `_recorder.start(const RecordConfig(), path: absoluteFilePath)` (default `RecordConfig` = `AudioEncoder.aacLc`, per D-11 — no custom codec/bitrate tuning) and starts a one-shot `Timer(const Duration(seconds: 60), ...)` (D-09) that, if still active when it fires, calls `stop()` internally and invokes `onAutoStop`. Method `Future<String?> stop()` cancels the pending 60s `Timer` if still active, guards against a second call (an internal `bool _stopping` flag: if already stopping/stopped, return `null` immediately — this is the "first stop signal wins" guard for the manual-tap-vs-auto-stop race noted in UI-SPEC), then calls `_recorder.stop()` and returns the finalized absolute path. Method `void dispose()` calls `_recorder.dispose()`.

    Create `lib/services/audio_player_service.dart` with class `AudioPlayerService` wrapping `package:audioplayers/audioplayers.dart`'s `AudioPlayer`. Method `Future<void> play(String absoluteFilePath)` calls `_player.play(DeviceFileSource(absoluteFilePath))`. Method `Future<void> stop()` calls `_player.stop()`. Method `void dispose()` calls `_player.dispose()`.

    Create `lib/data/questions.dart` exporting `const List<String> kQuestions` with exactly 5 hardcoded practice-question strings (Claude's Discretion per CONTEXT.md — e.g. everyday-topic prompts a spoken-English learner could answer in under 60 seconds; keep each under ~120 characters so it fits comfortably in the Display text style added in Plan 2).

    Create `lib/state/practice_state.dart` with `enum PracticePhase { idle, recording, saving, replaying, error }` and class `PracticeState extends ChangeNotifier`. Constructor accepts `RecordingService recordingService, AudioPlayerService audioPlayerService, DatabaseHelper databaseHelper` (manual constructor injection, no DI package — ARCHITECTURE.md Pattern 2; this is the seam Task 3's tests use to substitute fakes). Fields: `String currentQuestion`, `PracticePhase phase`, `String? errorMessage`, private `String? _lastQuestionIndex` (or similar) to avoid picking the same question twice in a row. Method `Future<void> startNewQuestion()`: picks the next question from `kQuestions` (avoid immediate repeat where possible), sets `phase = PracticePhase.recording`, `notifyListeners()`, computes a new absolute file path via `ensureRecordingsDir()` + a filename derived from `DateTime.now().millisecondsSinceEpoch` + `.m4a`, and calls `recordingService.start(absolutePath, onAutoStop: stopRecording)`. Method `Future<void> stopRecording()`: sets `phase = PracticeState.saving` (ignore if already not `recording`, matching the stop-guard in RecordingService), `notifyListeners()`, awaits `recordingService.stop()`; if the returned path is null, treat as a no-op (RecordingService already guarded the double-stop case — do not write a DB row or show an error here, Task 3 of Plan 3 covers genuine failures) and return. On a real finalized path, compute the DB-stored relative path (`recordings/<filename>.m4a`) and call `databaseHelper.insertAnsweredSession(questionText: currentQuestion, audioRelativePath: relativePath)` — this call must happen only after the awaited `stop()` has returned a non-null finalized path, never before. After the DB write completes, set `phase = PracticeState.replaying`, `notifyListeners()`, resolve the absolute path via `toAbsolutePath(relativePath)` and `await audioPlayerService.play(absolutePath)`, then call `startNewQuestion()` again (D-03 reset loop).

    Create `lib/screens/practice_screen.dart` as a `StatefulWidget` whose `State.initState()` constructs one `PracticeState` (owning one `RecordingService`/`AudioPlayerService`/`DatabaseHelper` instance each) and immediately calls `startNewQuestion()` (D-01 — no Start button, no confirmation). Wrap the body in `ListenableBuilder(listenable: _state, builder: ...)`. Show the current question text. Show a Stop button (`ElevatedButton` or `FilledButton` with label "STOP" and a leading `Icon(Icons.stop_rounded)`, per UI-SPEC's Copywriting Contract) ONLY when `phase == PracticePhase.recording`, calling `_state.stopRecording()` on tap — no elapsed timer or countdown display anywhere on this screen (D-04). Show a small "Playing your answer…" text (UI-SPEC Copywriting Contract) only when `phase == PracticePhase.replaying`. Add an `AppBar` with a History `IconButton` (`Icon(Icons.history)`) that navigates to `HistoryScreen` via `Navigator.push` (Plan 1 Task 2 creates `HistoryScreen`; this button just needs the import + route wiring, referencing the not-yet-styled screen is fine since Task 2 runs immediately after in the same plan). Call `_state.recordingService.dispose()` / `_state.audioPlayerService.dispose()` in `State.dispose()`.

    Rewrite `lib/main.dart` to `runApp(const EnglishReflexApp())` where `EnglishReflexApp` is a `StatelessWidget` returning `MaterialApp(title: 'EnglishReflex', home: const PracticeScreen())` (theming deferred to Plan 2 — this task only needs the app to build and run).

    Create `test/models/session_test.dart` and `test/models/question_answer_test.dart`: pure-Dart round-trip tests (`fromMap(toMap())` reproduces the original field values) — no Flutter widget/platform dependency, fast and fully automatable.

    Add `RECORD_AUDIO` Android permission and `NSMicrophoneUsageDescription` iOS permission is explicitly Task 3's responsibility in this plan — do not add manifest entries in this task.
  </action>
  <verify>
    <automated>cd . && flutter pub get && flutter analyze && flutter test test/models/session_test.dart test/models/question_answer_test.dart</automated>
    <human-check>
      <test>Run `flutter run` on an Android emulator (or connected device). On PracticeScreen: (1) confirm a question appears and recording starts automatically with no button tap and only a Stop button visible (no elapsed timer); (2) tap Stop after a few seconds and confirm the recording auto-plays back with the "Playing your answer…" label, then a new question appears and recording restarts automatically; (3) on a separate run, let a recording go the full duration without tapping Stop and confirm it auto-stops at 60 seconds via the internal Timer.</test>
      <expected>Recording auto-starts on screen open; manual Stop finalizes and triggers save+replay+reset; unattended recording auto-stops at 60s and follows the same save+replay+reset path.</expected>
      <why_human>Real microphone capture, real file finalization timing, and the 60s auto-stop cannot be exercised by a sandboxed automated test — this is the phase's core "reflex" mechanic and must be seen working on a real device/emulator.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` reports zero errors
    - `flutter test test/models/session_test.dart test/models/question_answer_test.dart` passes
    - `lib/db/database_helper.dart` contains `db.transaction(` wrapping both the `sessions` and `question_answers` inserts inside `insertAnsweredSession`
    - `lib/services/recording_service.dart` contains a `Timer(const Duration(seconds: 60)` (or equivalent 60-second one-shot deadline construct, not a periodic tick counter)
    - `lib/screens/practice_screen.dart` calls `startNewQuestion()` from `initState`
  </acceptance_criteria>
  <done>App builds and runs; opening PracticeScreen auto-starts a recording, manual Stop or the 60s auto-stop finalizes it, the session+answer row is written in one transaction only after finalization, auto-replay plays it back, and the screen resets to a new question and starts recording again.</done>
</task>

<task type="auto">
  <name>Task 2: History list + session detail — find and replay any past answer</name>
  <read_first>
    lib/db/database_helper.dart
    lib/screens/practice_screen.dart
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-UI-SPEC.md
  </read_first>
  <files>lib/db/database_helper.dart, lib/screens/history_screen.dart, lib/screens/session_detail_screen.dart, lib/main.dart, lib/screens/practice_screen.dart</files>
  <action>
    Extend `lib/db/database_helper.dart` (created in Task 1) with two read methods: `Future<List<Session>> listSessions()` running `db.query('sessions', orderBy: 'id DESC')` mapped through `Session.fromMap` (most-recent-first, HIST-01), and `Future<List<QuestionAnswer>> listAnswersForSession(int sessionId)` running `db.query('question_answers', where: 'session_id = ?', whereArgs: [sessionId], orderBy: 'id ASC')` mapped through `QuestionAnswer.fromMap` (HIST-02) — both use the typed `query()` convenience method with `whereArgs`, never a raw string-concatenated SQL statement.

    Create `lib/screens/history_screen.dart` as a `StatelessWidget` (or minimal `StatefulWidget` holding one `Future<List<Session>>`) that takes a `DatabaseHelper` instance and renders via `FutureBuilder`: if the resolved list is empty, show heading text "No recordings yet" and body text "Finish your first answer and it'll show up here." (UI-SPEC Copywriting Contract, HIST-01 empty case); otherwise render a `ListView.builder` of session rows (one row per `Session`, showing a formatted `createdAt` — e.g. via `DateFormat` from `intl` is NOT needed, use a small hand-written formatter or `createdAt.toLocal().toString()` truncated to date+time for now) where tapping a row navigates via `Navigator.push` to `SessionDetailScreen(sessionId: session.id!, databaseHelper: databaseHelper)`.

    Create `lib/screens/session_detail_screen.dart` as a `StatefulWidget` taking `sessionId` and a `DatabaseHelper`, with an `AppBar` title formatted as "Session — {date, time}" (e.g. "Session — Aug 7, 2:45 PM", UI-SPEC Copywriting Contract — derive from the session's `createdAt`, fetched via a small extra query or passed in from `HistoryScreen`). Body renders via `FutureBuilder(future: databaseHelper.listAnswersForSession(sessionId))`: a `ListView.builder` of one row per `QuestionAnswer`, each row showing `questionText`; tapping a row resolves the absolute path via `toAbsolutePath(answer.audioPath)` (from `lib/utils/audio_paths.dart`) and calls `AudioPlayerService.play(absolutePath)` on a locally-owned `AudioPlayerService` instance (HIST-03 — independent of whether Practice screen's auto-replay already played it once). Dispose the `AudioPlayerService` in `State.dispose()`.

    Wire `lib/main.dart`/`lib/screens/practice_screen.dart`'s History `IconButton` (added in Task 1) to actually push `HistoryScreen(databaseHelper: ...)` — reuse the same `DatabaseHelper` instance `PracticeState` already owns (pass it through, or construct a second `DatabaseHelper()` — since `openDatabase` opens the same file path either way, a second instance is safe and simpler than threading one instance through the widget tree for this phase's scope).
  </action>
  <verify>
    <automated>flutter analyze && flutter test test/models/session_test.dart test/models/question_answer_test.dart</automated>
    <human-check>
      <test>From PracticeScreen, tap the History icon. With zero recordings ever made (fresh install / cleared app data), confirm "No recordings yet" / "Finish your first answer and it'll show up here." appears. After recording at least two answers, reopen History and confirm both sessions appear, most-recent first. Tap a session, confirm its one question appears in detail with the title "Session — {date, time}". Tap the question row and confirm its own recording plays.</test>
      <expected>Empty state renders correctly with zero sessions; populated state lists all sessions most-recent-first; detail view scopes to exactly that session's rows and replays the correct recording per row.</expected>
      <why_human>Requires a real device/emulator with actual recorded audio files and a real navigation flow — not reproducible by a sandboxed automated test.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `lib/db/database_helper.dart` contains `listSessions` using `orderBy: 'id DESC'` and `listAnswersForSession` using `where: 'session_id = ?'` with `whereArgs`
    - `lib/screens/history_screen.dart` renders the exact strings "No recordings yet" and "Finish your first answer and it'll show up here." for the empty case
    - `lib/screens/session_detail_screen.dart` builds its `AppBar` title from the session's `createdAt`
    - `flutter analyze` reports zero errors
  </acceptance_criteria>
  <done>History screen lists all sessions most-recent-first (or the empty-state copy when none exist); tapping a session opens a detail view scoped to that session's question_answers rows; tapping a question row plays its own recording.</done>
</task>

<task type="auto">
  <name>Task 3: Automated state-machine tests, DB tests, and mobile mic-permission manifests</name>
  <read_first>
    lib/state/practice_state.dart
    lib/db/database_helper.dart
    lib/services/recording_service.dart
    lib/services/audio_player_service.dart
  </read_first>
  <files>test/state/practice_state_test.dart, test/db/database_helper_test.dart, android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist</files>
  <action>
    Create `test/db/database_helper_test.dart`. In `setUpAll`, call `sqfliteFfiInit()` (from `package:sqflite_common_ffi/sqflite_ffi.dart`) and set the package-level `databaseFactory = databaseFactoryFfi` — this redirects `DatabaseHelper`'s internal `openDatabase(...)` call to the FFI in-memory/desktop SQLite engine with zero changes to `DatabaseHelper` itself. Write tests against a fresh `DatabaseHelper()` instance per test (or reset between tests) covering: `insertAnsweredSession` returns an id and both rows exist afterward (`db.query('sessions')` has 1 row, `db.query('question_answers')` has 1 row with matching `session_id`); calling it twice produces two distinct sessions, each with exactly one linked answer (never zero, per the HIST-02-empty must-have); `listSessions()` returns sessions ordered most-recent-first; `listAnswersForSession(id)` returns only that session's row(s) and none from a second session.

    Create `test/state/practice_state_test.dart`. Define two small hand-written fakes in the test file (subclassing the real service classes, no mocking package needed): `class FakeRecordingService extends RecordingService` overriding `start`/`stop` to record call order into a `List<String> calls` and return a fixed fake file path from `stop()` without touching any platform channel; `class FakeAudioPlayerService extends AudioPlayerService` overriding `play` to record the call into the same-shaped list. Also fake `DatabaseHelper` is unnecessary here — instead point `PracticeState`'s injected `DatabaseHelper` at the FFI in-memory engine the same way `database_helper_test.dart` does (`sqfliteFfiInit()` + `databaseFactory = databaseFactoryFfi` in `setUpAll`), so this test exercises the real crash-safety-critical write path, not a faked one. Assert: after `startNewQuestion()`, `phase == PracticePhase.recording` and `FakeRecordingService.calls` contains a `start` entry; after `stopRecording()` resolves, the call order is `stop` (Fake recording) THEN a DB write THEN `play` (Fake audio player) THEN a second `start` (the D-03 reset) — i.e. assert the DB row exists (via a direct `DatabaseHelper.listSessions()` check) before `FakeAudioPlayerService.calls` records the `play` entry, proving the save-before-replay ordering from D-08/D-10 holds. Also assert `phase` ends back at `PracticePhase.recording` after the full cycle (the reset-loop truth, D-03).

    Add `<uses-permission android:name="android.permission.RECORD_AUDIO" />` to `android/app/src/main/AndroidManifest.xml` (inside the `<manifest>` element, alongside/above the existing `<application>` element — per the `record` package's documented Android permission requirement). Add the `NSMicrophoneUsageDescription` key to `ios/Runner/Info.plist`'s top-level `<dict>` with a short user-facing string value (e.g. "EnglishReflex needs microphone access to record your spoken practice answers.") — per the `record` package's documented iOS permission requirement.
  </action>
  <verify>
    <automated>flutter test test/db/database_helper_test.dart test/state/practice_state_test.dart && grep -c 'android.permission.RECORD_AUDIO' android/app/src/main/AndroidManifest.xml && grep -c 'NSMicrophoneUsageDescription' ios/Runner/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/db/database_helper_test.dart` passes, asserting a session always has exactly one linked answer immediately after `insertAnsweredSession`
    - `flutter test test/state/practice_state_test.dart` passes, asserting the DB write happens before `AudioPlayerService.play` is invoked
    - `android/app/src/main/AndroidManifest.xml` contains exactly one `android.permission.RECORD_AUDIO` entry
    - `ios/Runner/Info.plist` contains exactly one `NSMicrophoneUsageDescription` key
  </acceptance_criteria>
  <done>Automated tests prove the save-before-replay ordering and the "session always has its one answer" invariant without needing a real device; both platforms declare the mic permission the `record` package requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS microphone -> `RecordingService` | Raw audio capture from a real-world input; no validation possible or needed on the audio bytes themselves, but the resulting file's storage location and lifecycle are a trust boundary. |
| App code -> local filesystem/SQLite | The only persistence boundary in this phase (no network calls at all — Firestore is deferred to Phase 3). |
| App code -> `pubspec.yaml` third-party packages | Supply-chain boundary: `record`, `audioplayers`, `sqflite`, `path_provider`, `path`, `sqflite_common_ffi` are all installed from pub.dev. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `lib/utils/audio_paths.dart` / `lib/services/recording_service.dart` (audio file storage location) | high | mitigate | Store audio exclusively under `path_provider`'s app-private `getApplicationDocumentsDirectory()/recordings/` — never external/shared storage; no `WRITE_EXTERNAL_STORAGE` permission requested. |
| T-01-02 | Tampering | `lib/db/database_helper.dart` (SQL construction) | medium | mitigate | Use sqflite's typed `insert()`/`query()` convenience methods with `whereArgs` everywhere in this task; never build a SQL string via interpolation, even though Phase 1's only "input" is a hardcoded question list. |
| T-01-03 | Tampering | `lib/services/recording_service.dart` + `lib/db/database_helper.dart` write ordering | high | mitigate | `insertAnsweredSession` wraps both inserts in one `db.transaction()`, called only after `RecordingService.stop()` returns a finalized path — a crash mid-write can never leave an orphaned/partial row (D-08). |
| T-01-04 | Denial of Service | `lib/services/recording_service.dart` (unbounded recording) | low | accept | A recording that is never manually stopped is bounded by the 60-second one-shot `Timer` (D-09); worst case is one wasted file, not an unbounded resource drain. |
| T-01-SC | Tampering | `pubspec.yaml` (record, audioplayers, sqflite, path_provider, path, sqflite_common_ffi) | medium | mitigate | All six packages are already pre-vetted in `.planning/research/STACK.md`/CLAUDE.md except `path` and `sqflite_common_ffi`, both official/high-reputation, low-surface-area utility packages used only as documented (path-joining; FFI test harness) — no untrusted forks, no code-execution-at-install-time packages. |
</threat_model>

<verification>
Run `flutter analyze` and the full Task 1-3 automated test suite (`flutter test test/models test/db test/state`) with zero failures. On a real Android emulator/device, complete the human-check flows in Task 1 and Task 2: auto-record -> manual-stop-or-60s-auto-stop -> auto-replay -> reset-to-new-question loop; History list (empty and populated states); session detail with tap-to-replay.
</verification>

<success_criteria>
- `flutter run` launches the app to PracticeScreen, which immediately starts recording with no button tap.
- Stopping (manually or via the 60s cap) reliably saves the session+answer in one transaction, auto-replays it, and resets to a new question that starts recording again.
- History lists every saved session most-recent-first (or the empty-state copy), and every session's detail view replays its own recording correctly.
- `flutter analyze` and all Task 1-3 tests pass.
</success_criteria>

<output>
Create `.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-01-SUMMARY.md` when done
</output>
