---
phase: 01-record-save-replay-a-single-answer-crash-safe
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/widgets/mascot.dart
  - lib/screens/practice_screen.dart
  - lib/screens/history_screen.dart
  - lib/screens/session_detail_screen.dart
  - test/widgets/mascot_test.dart
  - .claude/CLAUDE.md
autonomous: true
requirements: [UI-01, UI-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "MaterialApp's theme uses the UI-SPEC color tokens exactly: #FFF8F0 dominant background, #FFDDB3 secondary surfaces (question card, history rows, mascot container), #FF6B35 accent (Stop button, recording pulse ring, play icons), #E5484D error/destructive, #3D2B1F text-on-color (UI-02)"
    - "Question prompt text renders in Baloo 2 at 32px SemiBold(600) with 1.3 line-height (Display role, UI-01/UI-02)"
    - "Screen titles (\"Exercise History\", session detail title) render in Baloo 2 at 24px SemiBold(600) with 1.2 line-height (Heading role, UI-02)"
    - "Stop button label and history-row action text render at 18px SemiBold(600) with 1.2 line-height in the default Material font (Label role) — Baloo 2 is reserved for Display/Heading only"
    - "History list metadata (date/time) renders at 16px Regular(400) with 1.5 line-height in the default Material font (Body role)"
    - "No screen sets textScaleFactor to a fixed 1.0 — text scales with the OS accessibility text-size setting"
    - statement: "The longest hardcoded question string (from lib/data/questions.dart) wraps across multiple lines at 32px Display size with no clipping/overflow, including at the largest OS text-scale accessibility setting"
      verification: backstop
    - "A recurring mic-with-face mascot (120px square) appears on the Practice screen with distinct idle vs. recording (listening) face variants, plus an accent-colored (#FF6B35) pulse ring shown only while recording"
    - "The Stop button renders as a 96px-diameter circular touch target; History list rows render at a 64px minimum height (UI-SPEC spacing exceptions for large touch targets, UI-01)"
    - "CLAUDE.md's Supporting Libraries table has a new row documenting google_fonts (per D-15 and the UI-SPEC package note)"
    - "Planner assumption (flagged, not gated): the edge-probe's 'encoding — whose definition of length/equality applies (bytes/code points/grapheme clusters/normalized form)' item does not meaningfully apply to this plan — Phase 1's question text is a fixed, plain-ASCII, hardcoded English list (lib/data/questions.dart) rendered through Flutter's default Unicode-aware text layout engine, so no custom encoding/normalization handling is required; revisit only if a future phase introduces non-ASCII or user-authored question text"
  artifacts:
    - path: "lib/widgets/mascot.dart"
      provides: "Mascot widget — 120px mic-with-face, idle/listening/error face variants, accent pulse ring while recording"
      exports: ["Mascot"]
    - path: "lib/main.dart"
      provides: "MaterialApp.theme built from the UI-SPEC color/typography tokens"
    - path: "pubspec.yaml"
      provides: "google_fonts dependency addition (D-15)"
  key_links:
    - from: "lib/screens/practice_screen.dart"
      to: "lib/widgets/mascot.dart"
      via: "PracticeScreen renders <Mascot isRecording={phase == PracticePhase.recording}> above the question text"
      pattern: "Mascot\\("
    - from: "lib/main.dart"
      to: "lib/screens/practice_screen.dart, lib/screens/history_screen.dart, lib/screens/session_detail_screen.dart"
      via: "Theme.of(context).textTheme roles (displayLarge/headlineSmall/labelLarge/bodyLarge) consumed by each screen's Text widgets instead of ad-hoc TextStyles"
      pattern: "Theme\\.of\\(context\\)\\.textTheme"
  prohibitions:
    - "The mascot's face variants and any recording-cutoff-adjacent copy must never read as mocking, disappointed, or shaming toward the user (e.g. no sad/frowning-at-the-user face tied to a short or interrupted answer) — the friendly/Duolingo-style direction (D-14) must stay encouraging, not evaluative, since this app has no scoring feature."
---

<objective>
Apply the locked warm/colorful/playful visual system (D-12–D-15, UI-SPEC) to the working screens Plan 1 built: theme colors, Baloo 2 typography for Display/Heading roles, a recurring mic-with-face mascot, and the large-touch-target spacing exceptions — with zero changes to the underlying record/save/replay/history logic.

Purpose: LOOP-03..06/PERSIST-01/02/HIST-01..04 are already true after Plan 1; this plan makes UI-01 ("large, easily readable text") and UI-02 ("simple, colorful, friendly/cartoon-like — not corporate/minimal-grey") true, satisfying the phase's remaining two requirements without touching persistence, recording, or navigation logic.

Output: PracticeScreen/HistoryScreen/SessionDetailScreen render with the UI-SPEC color palette, Baloo 2 typography, a mascot with a recording-state pulse ring, and the 96px Stop button / 64px history row touch targets.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-UI-SPEC.md
@.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme (colors + typography) applied to all three screens</name>
  <read_first>
    lib/main.dart
    lib/screens/practice_screen.dart
    lib/screens/history_screen.dart
    lib/screens/session_detail_screen.dart
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-UI-SPEC.md
  </read_first>
  <files>pubspec.yaml, lib/main.dart, lib/screens/practice_screen.dart, lib/screens/history_screen.dart, lib/screens/session_detail_screen.dart, .claude/CLAUDE.md</files>
  <action>
    Add `google_fonts: ^8.1.0` (or the current resolved caret version from `flutter pub add google_fonts`) to `pubspec.yaml` dependencies and run `flutter pub get`. Add one row to `.claude/CLAUDE.md`'s "Supporting Libraries" table for `google_fonts`, using the exact wording from UI-SPEC's package note: it loads the Baloo 2 Google Font for headings/question text only (D-15), a single font-family package addition rather than a UI framework.

    In `lib/main.dart`, build the `MaterialApp.theme` from `ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFFFF6B35), brightness: Brightness.light).copyWith(primary: const Color(0xFFFF6B35), surface: const Color(0xFFFFDDB3), error: const Color(0xFFE5484D), onSurface: const Color(0xFF3D2B1F)), scaffoldBackgroundColor: const Color(0xFFFFF8F0), textTheme: <built from GoogleFonts.baloo2TextTheme for displayLarge/headlineSmall, default textTheme for labelLarge/bodyLarge>)`. Concretely: set `textTheme.displayLarge` to `GoogleFonts.baloo2(fontSize: 32, fontWeight: FontWeight.w600, height: 1.3, color: const Color(0xFF3D2B1F))` (Display role — question prompt text), `textTheme.headlineSmall` to `GoogleFonts.baloo2(fontSize: 24, fontWeight: FontWeight.w600, height: 1.2, color: const Color(0xFF3D2B1F))` (Heading role — screen titles), `textTheme.labelLarge` to a default-font `TextStyle(fontSize: 18, fontWeight: FontWeight.w600, height: 1.2, color: const Color(0xFF3D2B1F))` (Label role — Stop button, history-row action text, no Baloo 2), `textTheme.bodyLarge` to a default-font `TextStyle(fontSize: 16, fontWeight: FontWeight.w400, height: 1.5, color: const Color(0xFF3D2B1F))` (Body role — history metadata). Do not set `MaterialApp.builder`'s `MediaQuery` to override `textScaler`/`textScaleFactor` to a fixed value anywhere — the app must inherit the OS accessibility text-scale setting (UI-01 backstop).

    Update `lib/screens/practice_screen.dart` to consume `Theme.of(context).textTheme.displayLarge` for the question text and `Theme.of(context).textTheme.labelLarge` for the Stop button label, and wrap the question card in a `Container`/`Card` using `Theme.of(context).colorScheme.surface` (`#FFDDB3`) as its background with rounded corners (large `borderRadius`, e.g. 24). Style the Stop button as a 96px-diameter circular target (`SizedBox(width: 96, height: 96, child: <circular button using colorScheme.primary #FF6B35 as fill>)`).

    Update `lib/screens/history_screen.dart` to use `Theme.of(context).textTheme.headlineSmall` for the "Exercise History" AppBar title, `Theme.of(context).textTheme.bodyLarge` for row date/time metadata, and give each row a minimum height of 64px (`ConstrainedBox`/`SizedBox(height: 64)` around each `ListTile`/row) with the accent color (`#FF6B35`) used only on the row's play-icon affordance, never as a decorative fill.

    Update `lib/screens/session_detail_screen.dart` similarly: `Theme.of(context).textTheme.headlineSmall` for the "Session — {date, time}" AppBar title, `Theme.of(context).textTheme.displayLarge`-scale-appropriate (or `bodyLarge`, since question text at detail-row scale is smaller than the live Practice-screen prompt — use `bodyLarge` here) for each question row's text, accent-colored play icon per row.
  </action>
  <verify>
    <automated>flutter analyze && flutter pub deps | grep -c google_fonts</automated>
    <human-check>
      <test>Run the app; visually confirm the ivory (#FFF8F0) background, peach (#FFDDB3) cards/rows, coral-orange (#FF6B35) Stop button and play icons, and Baloo 2 rendering on the question text and screen titles. In the OS accessibility settings, set text size to its largest setting and reopen the Practice screen; confirm the longest question string wraps to multiple lines without clipping or overflow.</test>
      <expected>Colors match the UI-SPEC token table exactly; Baloo 2 renders on Display/Heading text only; text reflows (not clips) at the largest OS text-scale setting.</expected>
      <why_human>Visual color/font rendering and OS-level text-scale behavior cannot be verified by a headless automated check.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` lists `google_fonts` as a dependency
    - `lib/main.dart` sets `scaffoldBackgroundColor` to `Color(0xFFFFF8F0)` and a `colorScheme` with `primary`/`error` set to `Color(0xFFFF6B35)`/`Color(0xFFE5484D)`
    - `lib/main.dart` does not set `textScaleFactor` or `MediaQuery`'s `textScaler` to a fixed value anywhere
    - `.claude/CLAUDE.md`'s Supporting Libraries table includes a `google_fonts` row
    - `flutter analyze` reports zero errors
  </acceptance_criteria>
  <done>All three screens render with the UI-SPEC warm palette and Baloo 2 Display/Heading typography, and text scales with the OS accessibility setting instead of a fixed factor.</done>
</task>

<task type="auto">
  <name>Task 2: Mascot widget with recording-state pulse ring</name>
  <read_first>
    lib/screens/practice_screen.dart
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-UI-SPEC.md
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-CONTEXT.md
  </read_first>
  <files>lib/widgets/mascot.dart, lib/screens/practice_screen.dart, test/widgets/mascot_test.dart</files>
  <action>
    Create `lib/widgets/mascot.dart` exporting `class Mascot extends StatefulWidget` with constructor params `{super.key, this.isRecording = false, this.isError = false}`. Render a 120x120 `Container` with `BoxShape.circle`, background `Color(0xFFFFDDB3)` (secondary/peach), containing a simple "face": a centered `Icon(Icons.mic_rounded, size: 56, color: Color(0xFF3D2B1F))` for the mouth/mic element plus two small circular "eyes" positioned via `Stack`/`Positioned` (e.g. two 8px `Color(0xFF3D2B1F)` dots) — this satisfies D-13's "mic-with-face" direction without needing an external image asset. When `isError` is true, swap the eyes to a distinct "concerned but not sad/mocking" variant (e.g. slightly angled dashes instead of circles) paired with the error-state copy Plan 3 adds — never a frowning/disappointed expression (per this plan's prohibition). When `isRecording` is true, wrap the container in an outer `AnimatedContainer`/`AnimationController`-driven ring: a circular border in `Color(0xFFFF6B35)` (accent) that pulses (scale or opacity animation, e.g. 1.0 to 1.15 scale over ~800ms, repeating) — use a `SingleTickerProviderStateMixin` `AnimationController` that starts in `initState` when `isRecording` is true and is disposed in `State.dispose()`; stop/restart it when `isRecording` changes via `didUpdateWidget`.

    In `lib/screens/practice_screen.dart`, render `Mascot(isRecording: state.phase == PracticePhase.recording, isError: state.phase == PracticePhase.error)` positioned above the question text (using the UI-SPEC spacing scale's `xl` token, 32px, as the gap between mascot and question text).

    Create `test/widgets/mascot_test.dart`: a `WidgetTester` test that pumps `Mascot(isRecording: false)` and `Mascot(isRecording: true)` inside a `MaterialApp`/`Scaffold` and asserts both build without throwing, and that the accent-colored pulse-ring element (e.g. find by a `Key('mascot-pulse-ring')` you add to that widget) is present in the widget tree only when `isRecording: true` and absent when `isRecording: false`.
  </action>
  <verify>
    <automated>flutter test test/widgets/mascot_test.dart</automated>
    <human-check>
      <test>On the Practice screen, watch the mascot while a recording is active vs. idle/replaying.</test>
      <expected>An accent-colored pulse ring animates around the mascot only while actively recording; it is absent immediately after Stop fires.</expected>
      <why_human>Animation timing/visual pulse cannot be asserted by a widget test beyond presence/absence of the ring element.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widgets/mascot_test.dart` passes
    - `lib/widgets/mascot.dart` exports `Mascot` with `isRecording` and `isError` constructor parameters
    - `lib/screens/practice_screen.dart` renders `Mascot(` bound to `state.phase`
  </acceptance_criteria>
  <done>A 120px mic-with-face mascot appears on the Practice screen, with an accent-colored pulse ring that shows only while recording is active, and a distinct (non-mocking) face variant reserved for the error state Plan 3 wires up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App code -> `pubspec.yaml` (new `google_fonts` dependency) | Supply-chain boundary: a new third-party package added specifically in this plan. |
| No new data/storage/network boundary | This plan is purely presentational (theme, typography, a bundled-asset-free mascot widget); it introduces no new trust boundary around data flow, only a new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `pubspec.yaml` (`google_fonts` addition) | medium | mitigate | Add only the official `google_fonts` package (pub.dev, `dart.dev`-affiliated publisher, very high pub-points/likes, matches the exact package UI-SPEC and D-15 name) — no forked/unofficial "googlefonts"-lookalike package; pin with a caret range, not an unpinned `any`. |
| T-02-02 | Denial of Service | `lib/widgets/mascot.dart` (custom asset/animation) | low | accept | The mascot is composed entirely from Flutter's built-in `Icon`/`Container`/`AnimationController` primitives — no network-fetched image, no unbounded animation loop beyond a simple repeating pulse tied to `isRecording`. |
| T-02-03 | Information Disclosure | none — no new data surface | low | accept | This plan touches only theme/typography/a presentational widget; no new data is read, stored, or displayed beyond what Plan 1 already surfaces. |
</threat_model>

<verification>
Run `flutter analyze` and `flutter test test/widgets/mascot_test.dart`. On a real device/emulator, visually confirm the UI-SPEC color palette, Baloo 2 typography on Display/Heading text, the mascot's recording-state pulse ring, and correct text reflow at the largest OS accessibility text-scale setting.
</verification>

<success_criteria>
- All three screens (Practice, History, Session Detail) render with the UI-SPEC warm color palette and Baloo 2 Display/Heading typography.
- The Practice screen shows a 120px mascot with an accent pulse ring only while recording.
- Text scales with the OS accessibility setting; the longest hardcoded question wraps without clipping at the largest text-scale setting.
- `flutter analyze` and `flutter test test/widgets/mascot_test.dart` pass.
</success_criteria>

<output>
Create `.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-02-SUMMARY.md` when done
</output>
