---
phase: 01-record-save-replay-a-single-answer-crash-safe
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/services/recording_service.dart
  - lib/state/practice_state.dart
  - lib/screens/practice_screen.dart
  - lib/db/database_helper.dart
  - test/state/practice_state_test.dart
  - test/db/database_helper_test.dart
autonomous: true
requirements: [PERSIST-01, PERSIST-02, HIST-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "If mic permission is denied (RecordingService.hasPermission() returns false), PracticeState.phase becomes PracticePhase.error with errorMessage set to the exact UI-SPEC copy \"Recording failed — check your microphone permission and try again.\", and PracticeScreen shows that copy in an error banner instead of crashing"
    - "The error banner never surfaces a raw exception message, stack trace, or file path to the user — only the fixed UI-SPEC copy string"
    - "A visible Retry affordance on the error banner calls startNewQuestion() again, returning the app to a normal recording attempt"
    - "database_helper.dart opens the database with foreign key enforcement on (PRAGMA foreign_keys = ON via onConfigure), so a question_answers row can never reference a non-existent session id at the DB layer (defense-in-depth for PERSIST-02/D-08)"
    - "All SQL access in database_helper.dart continues to use sqflite's typed insert()/query() convenience methods with whereArgs — zero raw, string-interpolated SQL statements (T-01-02 audit, re-confirmed after this plan's edits)"
    - "If the app process is force-killed while a recording is actively in progress (before Stop has fully finalized the file and the transaction has committed), relaunching shows zero trace of that in-flight recording — no session row, no question_answers row, no orphaned audio file (D-08, PERSIST-02)"
    - statement: "Force-killing the app process AFTER at least one question has been fully answered (session+answer committed), then relaunching, shows that prior answer intact in History with a working recording (D-07 explicit manual crash-safety test)"
      verification: backstop
    - "Relaunching the app multiple times in a row without recording anything new shows the identical History list each time — listSessions()/listAnswersForSession() are pure reads with no side effects, so repeated app launches never duplicate or mutate existing rows (HIST-04 idempotency)"
  artifacts:
    - path: "lib/services/recording_service.dart"
      provides: "hasPermission()-gated start() with a caught-and-surfaced permission-denied path instead of an unhandled exception"
    - path: "lib/state/practice_state.dart"
      provides: "PracticePhase.error handling wired from RecordingService failures, plus a retry() method"
    - path: "lib/db/database_helper.dart"
      provides: "onConfigure enabling PRAGMA foreign_keys = ON"
  key_links:
    - from: "lib/services/recording_service.dart"
      to: "lib/state/practice_state.dart"
      via: "RecordingService.start() throws a typed exception on permission denial; PracticeState.startNewQuestion() catches it and sets phase = PracticePhase.error"
      pattern: "catch \\(.*\\).*PracticePhase\\.error"
    - from: "lib/state/practice_state.dart"
      to: "lib/screens/practice_screen.dart"
      via: "PracticeScreen reads state.phase == PracticePhase.error and state.errorMessage to render the error banner + Retry button"
      pattern: "PracticePhase\\.error"
  prohibitions:
    - "The error banner and any retry/failure copy must never expose internal implementation detail (file paths, exception class names, stack traces) to the end user — only the fixed, pre-approved UI-SPEC copy string."
    - "A failed or denied recording attempt must never write a partial/placeholder session or question_answers row — the transactional insertAnsweredSession() call must remain reachable only from a fully successful RecordingService.stop() result, exactly as in Plan 1."
---

<objective>
Harden the crash-safety-critical path built in Plan 1 with explicit error handling (mic-permission-denied, save failure) and a defense-in-depth DB constraint, then prove PERSIST-02's core reliability claim with the explicit manual force-kill test D-07 requires.

Purpose: Plan 1 proved the happy path; this plan closes the two gaps a "looks done but isn't" checklist would catch (PITFALLS.md: real permission-denial handling, and an explicit, deliberate crash test — not just "it worked when I closed the app normally").

Output: PracticeScreen shows a real, non-crashing error state on mic-permission-denial with a Retry action; the DB enforces foreign-key integrity as a second line of defense; the phase's defining risk (force-kill mid-session) is explicitly verified.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-CONTEXT.md
@.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-UI-SPEC.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mic-permission-denied and save-failure handling with a real error banner</name>
  <read_first>
    lib/services/recording_service.dart
    lib/state/practice_state.dart
    lib/screens/practice_screen.dart
  </read_first>
  <files>lib/services/recording_service.dart, lib/state/practice_state.dart, lib/screens/practice_screen.dart, test/state/practice_state_test.dart</files>
  <action>
    In `lib/services/recording_service.dart`, change `start()` to first `await hasPermission()`; if it returns `false`, throw a small typed exception (e.g. `class RecordingPermissionDeniedException implements Exception {}` declared in the same file) instead of calling `_recorder.start(...)` — this replaces Plan 1's minimal "just don't crash" behavior with a caller-catchable signal.

    In `lib/state/practice_state.dart`, wrap `startNewQuestion()`'s call to `recordingService.start(...)` in a `try`/`catch`: on `RecordingPermissionDeniedException` (or any other exception from `start()`), set `phase = PracticePhase.error`, `errorMessage = 'Recording failed — check your microphone permission and try again.'` (the exact UI-SPEC Copywriting Contract string — verbatim, no paraphrase), and `notifyListeners()` — do not attempt any DB write in this path. Add a `Future<void> retry()` method that simply calls `startNewQuestion()` again (used by the error banner's Retry action). Also wrap the `stopRecording()` method's `databaseHelper.insertAnsweredSession(...)` call in a `try`/`catch`: on any exception, set the same `phase = PracticePhase.error` / `errorMessage` (reusing the identical UI-SPEC copy — the user-facing message doesn't need to distinguish record-permission failures from save failures) and `notifyListeners()`, without calling `startNewQuestion()` again (avoid retrying into another recording attempt automatically after a save failure — the user-triggered Retry button handles that).

    In `lib/screens/practice_screen.dart`, when `state.phase == PracticePhase.error`, render an error banner (e.g. `MaterialBanner` or a colored `Container` docked at the top) showing `state.errorMessage` in the error color (`Color(0xFFE5484D)` from Plan 2's theme) with a "Retry" `TextButton`/`ElevatedButton` that calls `state.retry()`. Ensure the question card and mascot remain visible/stable underneath the banner rather than being replaced by a blank screen (UI-SPEC: "question screen stays visible so the user isn't stuck").

    Extend `test/state/practice_state_test.dart` with a new test: a `FakeRecordingService` variant (or a constructor flag on the existing fake) whose `start()` throws `RecordingPermissionDeniedException`; assert that after calling `startNewQuestion()`, `phase == PracticePhase.error` and `errorMessage == 'Recording failed — check your microphone permission and try again.'` exactly, and that no session row was written (`DatabaseHelper.listSessions()` remains empty afterward — this test runs against the same FFI in-memory DB setup Plan 1's test file established). Add a second test asserting `retry()` transitions `phase` back to `PracticePhase.recording`.
  </action>
  <verify>
    <automated>flutter analyze && flutter test test/state/practice_state_test.dart</automated>
    <human-check>
      <test>On a real device, deny microphone permission when first prompted (or revoke it in OS Settings before launching), then open PracticeScreen.</test>
      <expected>An error banner reading "Recording failed — check your microphone permission and try again." appears with a Retry button; the app does not crash and the question card remains visible underneath.</expected>
      <why_human>Real OS permission-denial flows cannot be triggered by a sandboxed automated test.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `flutter test test/state/practice_state_test.dart` passes, including the new permission-denied and retry cases
    - `lib/state/practice_state.dart` never calls `databaseHelper.insertAnsweredSession` from the permission-denied catch branch
    - `lib/screens/practice_screen.dart` renders `state.errorMessage` when `state.phase == PracticePhase.error`
    - `flutter analyze` reports zero errors
  </acceptance_criteria>
  <done>Mic-permission-denial and save failures produce a stable, non-crashing error banner with the exact UI-SPEC copy and a working Retry action, verified by both an automated fake-service test and a real-device permission-denial check.</done>
</task>

<task type="auto">
  <name>Task 2: Foreign-key enforcement, SQL-parameterization audit, and the explicit D-07 force-kill test</name>
  <read_first>
    lib/db/database_helper.dart
    test/db/database_helper_test.dart
    .planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-CONTEXT.md
  </read_first>
  <files>lib/db/database_helper.dart, test/db/database_helper_test.dart</files>
  <action>
    In `lib/db/database_helper.dart`, add an `onConfigure: (db) async { await db.execute('PRAGMA foreign_keys = ON'); }` callback to the `openDatabase(...)` call (SQLite does not enforce `REFERENCES` constraints by default; this makes the `question_answers.session_id REFERENCES sessions(id)` declaration from Plan 1 actually reject an insert against a non-existent session id, as a second line of defense behind the transaction). Re-read the file end-to-end and confirm every read/write method (`insertAnsweredSession`, `listSessions`, `listAnswersForSession`) still uses only `insert()`/`query()`/`db.transaction()`'s typed `txn.insert()` — if any method was written with a raw SQL string anywhere, convert it to the typed convenience method now (T-01-02 mitigation, final audit pass for this phase).

    Extend `test/db/database_helper_test.dart` with a new test asserting that attempting to insert a `question_answers` row with a `session_id` that does not exist in `sessions` throws (via the FFI database with `PRAGMA foreign_keys = ON` applied the same way `DatabaseHelper.onConfigure` applies it) — proving the foreign-key constraint is actually enforced, not just declared.
  </action>
  <verify>
    <automated>flutter test test/db/database_helper_test.dart</automated>
    <human-check>
      <test>D-07's explicit crash-safety test: on a real device/emulator, (1) start a recording, then force-kill the app process (swipe from the recents/app-switcher list, or `adb shell am force-stop com.englishreflex`) BEFORE tapping Stop — relaunch and confirm History shows no trace of that in-flight recording (no new session, no orphaned file). (2) Separately, fully answer at least one question (let it save and auto-replay), THEN force-kill the app, then relaunch — confirm that prior answer is still present in History with a working, replayable recording.</test>
      <expected>Case 1: zero trace of the in-flight recording on relaunch. Case 2: the already-completed answer survives the force-kill intact and replayable.</expected>
      <why_human>This is the phase's single highest-risk requirement (PERSIST-02) and CONTEXT.md D-07 explicitly requires it be proven via a real force-kill on a real process, not inferred from unit tests alone.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `flutter test test/db/database_helper_test.dart` passes, including the new foreign-key-rejection case
    - `lib/db/database_helper.dart` contains an `onConfigure` callback executing `PRAGMA foreign_keys = ON`
    - No method in `lib/db/database_helper.dart` builds a SQL string via interpolation
  </acceptance_criteria>
  <done>The DB enforces referential integrity as a second line of defense behind the transaction; the D-07 force-kill test has been performed and confirms both "in-flight recording leaves no trace" and "already-saved answers survive a force-kill."</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App code -> local filesystem/SQLite (hardening pass) | Same boundary as Plan 1; this plan adds a second enforcement layer (FK constraint) and closes the error-path gap that could otherwise leak internal state to the user. |
| App code -> user-visible error copy | A new boundary this plan introduces: what internal failure detail is allowed to reach the error banner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `lib/db/database_helper.dart` (referential integrity) | medium | mitigate | `PRAGMA foreign_keys = ON` via `onConfigure` rejects any `question_answers` row referencing a non-existent `sessions.id`, closing the gap where a transaction bug could otherwise silently create an orphaned reference. |
| T-03-02 | Information Disclosure | `lib/screens/practice_screen.dart` (error banner) | medium | mitigate | The error banner renders only the fixed UI-SPEC copy string, never a raw exception message, stack trace, or file path — preventing accidental disclosure of internal file-system layout or library internals to the user. |
| T-03-03 | Denial of Service | `lib/state/practice_state.dart` (permission-denied loop) | low | accept | A denied-permission state requires an explicit user tap (Retry) to re-attempt recording — there is no automatic retry loop that could spin or drain battery. |
| T-03-04 | Repudiation | N/A — single local user, no auth, no shared/multi-user state | low | accept | Not applicable at this app's scale (PROJECT.md: single local user, no accounts); no action is taken based on this threat. |
</threat_model>

<verification>
Run `flutter analyze` and `flutter test test/state/practice_state_test.dart test/db/database_helper_test.dart`. Perform both halves of the D-07 human-check on a real device/emulator: force-kill mid-recording (expect zero trace) and force-kill after a completed answer (expect the answer survives intact).
</verification>

<success_criteria>
- Mic-permission-denied and save failures produce a stable error banner with the exact UI-SPEC copy and a working Retry action — no crash, no silent failure.
- The database rejects any orphaned `question_answers` row via `PRAGMA foreign_keys = ON`, verified by an automated test.
- The D-07 force-kill test has been performed and both its cases (in-flight loss, completed-answer survival) hold.
- `flutter analyze` and all Plan 3 tests pass.
</success_criteria>

<output>
Create `.planning/phases/01-record-save-replay-a-single-answer-crash-safe/01-03-SUMMARY.md` when done
</output>
