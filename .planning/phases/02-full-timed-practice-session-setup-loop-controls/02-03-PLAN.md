---
phase: 02-full-timed-practice-session-setup-loop-controls
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/screens/setup_screen.dart
  - test/screens/setup_screen_test.dart
autonomous: true
requirements: [SETUP-02, SETUP-03, SETUP-04, SETUP-05, SETUP-06, SETUP-07]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- Goal-backward ---
    - "The user can pick a CEFR level from six single-select chips (A1 A2 B1 B2 C1 C2) with B1 pre-selected (SETUP-02 / D-17)."
    - "The user can set questionCount anywhere from 1 to 100 with a slider whose default is 10 (SETUP-03 / D-16)."
    - "The user can set the pre-record countdown `t` anywhere from 3 to 30 seconds with a slider whose default is 5 (SETUP-04 / D-16)."
    - "The user can set the max recording duration `d` anywhere from 10 to 120 seconds with a slider whose default is 60 (SETUP-05 / D-16)."
    - "The user can toggle auto-replay `r`, defaulting to ON (SETUP-06 / D-16)."
    - "All five settings plus the checked topics are carried into the pushed session as one immutable SessionConfig; START SESSION stays gated on at least one topic (SETUP-07 / D-19)."
    - "Every visit to Setup starts from the fixed defaults - nothing is remembered between sessions and no new persistence surface is introduced (D-18)."
    # --- Edge coverage: SETUP-03 (probe-derived) ---
    - "SETUP-03/boundary: the questionCount slider reaches exactly 1 and exactly 100 and cannot represent 0 or 101."
    - "SETUP-03/precision: divisions 99 makes every reachable questionCount an exact integer; the readout renders a bare numeral with no decimal point and no rounding step."
    # --- Edge coverage: SETUP-04 (probe-derived) ---
    - "SETUP-04/adjacency: `t` at its minimum of 3 still produces a full 3-2-1 reading countdown that remains visually distinct from the get-ready countdown, because the two occupy opposite slots (D-22)."
    - "SETUP-04/empty: `t` can never be null or zero - the slider always holds a value and its default is 5."
    - "SETUP-04/ordering: changing `t` before Start applies to every question in the session including the first; it is read once from SessionConfig and never re-read mid-session."
    # --- Edge coverage: SETUP-05 (probe-derived) ---
    - "SETUP-05/boundary: the `d` slider reaches exactly 10 and exactly 120 and cannot represent 9 or 121."
    - "SETUP-05/adjacency: a manual STOP landing in the same frame as the `d` deadline yields exactly one saved answer - first signal wins, second is a no-op."
    - "SETUP-05/empty: `d` can never be null or zero; a recording is never armed without a deadline."
    - "SETUP-05/ordering: the `d` deadline starts only once the microphone is genuinely live, after the arming window - never at the start of the `t` countdown (D-20)."
    - "SETUP-05/precision: `d` is whole-second granularity and the on-screen remaining readout and the deadline are the same object, so they can never disagree (D-21)."
    # --- Edge coverage: SETUP-06 (probe-derived) ---
    - "SETUP-06/adjacency: with `r` on the inter-question countdown starts only after replay completes; with `r` off it starts immediately after the commit."
    - "SETUP-06/empty: `r` always holds a boolean; its default is ON, matching the Phase 1 loop the user already knows."
    - "SETUP-06/ordering: `r` is read from SessionConfig, so its value is fixed for the whole session and cannot change part-way through."
    - "SETUP-06/idempotency: toggling `r` twice on Setup returns to the original value and starts an identical session."
    - statement: "SETUP-06/concurrency: a replay that is paused or interrupted and then resumed continues in place and never restarts the answer from the beginning."
      verification: backstop
    # --- UI Considerations: E2 CEFR level chip row ---
    - "E2/empty: the six CEFR levels are a fixed closed set - the row can never be empty."
    - "E2/loading: constant data and local selection state; there is no async path."
    - "E2/error: selection cannot fail, so there is no failure surface."
    - "E2/populated: six ChoiceChips with B1 pre-selected; unselected peach fill with brown label, selected coral fill with brown label, showCheckmark false."
    - "E2/partial: single-select is an invariant - exactly one chip is always selected, so there is no nothing-chosen state to design."
    - "E2/overflow: Wrap(spacing: 8, runSpacing: 8) wraps to two or more lines at large text scale, and that wrap is the specified behaviour rather than a failure (D-17)."
    - "E2/zero-one-many: always exactly six chips and always exactly one selected - the axis is fixed."
    - "E2/long-text: chip labels are two-character constants and cannot grow."
    # --- UI Considerations: E3 the three numeric readout+slider settings ---
    - "E3/empty: a slider always carries a value and the D-16 defaults are the first-frame state; there is no unfilled state."
    - "E3/loading: local state only, no async."
    - "E3/error: min, max and divisions make an out-of-range value unrepresentable, so no validation-error treatment is needed - the constraint is enforced by the widget, not by a message."
    - "E3/partial: all three settings always hold values, so the form cannot be partially filled."
    - "E3/long-text: readouts top out at 120 sec at Display 32 centred with no wrap risk, and screen readers receive the semanticFormatterCallback strings rather than the bare numeral."
    # --- UI Considerations: E4 auto-replay toggle ---
    - "E4/empty: a boolean with a default of ON always has a value."
    - "E4/loading: local state only."
    - "E4/error: there is no failure path."
    - "E4/populated: a SwitchListTile with a coral active track in a 64px row, with the Body helper line beneath it."
    - "E4/partial: a boolean has no partial state."
    - "E4/overflow: the tile sits inside the Setup scroll view; its label and helper wrap and the tile grows."
    - "E4/zero-one-many: exactly one toggle - there are no collection semantics."
    - "E4/long-text: the copy is fixed and the title wraps inside the tile at large text scale."

  artifacts:
    - path: "lib/screens/setup_screen.dart"
      provides: "The complete session-configuration form: topics, level, three numeric settings, replay toggle, gated Start"
      contains: "ChoiceChip"
    - path: "test/screens/setup_screen_test.dart"
      provides: "Widget tests for defaults, ranges, the Start gate and text-scale containment"
      min_lines: 80

  key_links:
    - from: "lib/screens/setup_screen.dart"
      to: "lib/models/session_config.dart"
      via: "START SESSION builds one SessionConfig from all six local fields"
      pattern: "SessionConfig\\("
    - from: "lib/screens/setup_screen.dart"
      to: "lib/screens/practice_screen.dart"
      via: "Navigator.push with the built config"
      pattern: "PracticeScreen\\(\\s*config:"

  prohibitions:
    - statement: "MUST NOT persist, cache, log or transmit the user's session configuration or topic selections anywhere - Setup holds them in widget state only and forgets them when the screen is left (D-18)."
      category: privacy
---

<objective>
Complete the Setup screen: the CEFR chip row, the three large numeric readout-plus-slider
settings, and the auto-replay toggle, all wired into the `SessionConfig` that plan 02-01
already carries into a session.

Purpose: plan 02-01's tracer delivered topics and the gated Start; this plan delivers the
remaining five configurable values so a session is genuinely configured rather than run on
defaults. SETUP-02 through SETUP-06 land here.

Output: a Setup screen that matches the UI-SPEC control table exactly and produces every
reachable SessionConfig.
</objective>

<execution_context>
@/Users/tranhao/Desktop/english_practice_app/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranhao/Desktop/english_practice_app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-CONTEXT.md
@.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-UI-SPEC.md
@.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-PATTERNS.md
@.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The five remaining Setup controls, exactly per the UI-SPEC control table</name>
  <read_first>
    - `.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-UI-SPEC.md` sections "Copywriting Contract → Setup screen copy (complete)", "Layout Contract → Setup screen" (the ASCII layout and the Setup control specifications table), "Color" (the colour-discipline rules, especially "peach never sits on peach" and "at most one large coral surface per screen"), "Spacing Scale" and "Typography"
    - `lib/screens/setup_screen.dart` as left by plan 02-01
    - `lib/main.dart` lines 79-131 (the single palette and text theme every style must come from)
    - `lib/screens/history_screen.dart` lines 145-188 (the keyed private sub-widget pattern and the 64x48 touch-target floor comment style)
    - `.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-PATTERNS.md` section "No Analog Found" - none of `Slider`, `ChoiceChip`, `SwitchListTile` or `CheckboxListTile` exists anywhere in `lib/` today, so the UI-SPEC control table is the only spec
  </read_first>
  <files>lib/screens/setup_screen.dart</files>
  <action>
Add the five remaining control groups to `SetupScreen`, in the order and with the spacing the
Layout Contract's ASCII layout fixes: topics (already built), 32px, Level, 32px, Questions,
32px, Thinking time, 32px, Answer length, 32px, the replay toggle. Section labels render at
`labelLarge`; helper lines at `bodyLarge`; numeric readouts at `displayLarge`, centred.

Level (SETUP-02 / D-17): a `Wrap(spacing: 8, runSpacing: 8)` of six `ChoiceChip`s labelled
`A1` `A2` `B1` `B2` `C1` `C2`, single-select with `B1` selected on first frame,
`showCheckmark: false`, unselected peach fill with a brown label and no border, selected
coral fill with a brown label. Helper line: `CEFR level of the questions you'll get.` The
chip row sits directly on the ivory background, NOT inside a peach card, because unselected
peach chips on a peach card would vanish.

Questions (SETUP-03): section label `Questions`, readout `{n}` as a bare numeral, `Slider`
with `min: 1, max: 100, divisions: 99`, initial value 10. No `label:` value-indicator popup -
the always-visible Display readout replaces it. No helper line.

Thinking time (SETUP-04): section label `Thinking time`, helper `How long you get to read the
question before recording starts.`, readout `{n} sec`, `Slider` with `min: 3, max: 30,
divisions: 27`, initial value 5.

Answer length (SETUP-05): section label `Answer length`, helper `Recording stops
automatically after this long.`, readout `{n} sec`, `Slider` with `min: 10, max: 120,
divisions: 110`, initial value 60.

All three sliders: coral active track and thumb, peach inactive track, all from
`Theme.of(context).colorScheme`. Each carries a `semanticFormatterCallback` producing
`{n} questions`, `{n} seconds thinking time` and `{n} seconds answer length` respectively, so
a screen reader announces meaning rather than a bare number. The unit format is `{n} sec` with
no pluralisation branch - `t` starts at 3 and `d` at 10, so a singular reading is unreachable.

Auto-replay (SETUP-06): a `SwitchListTile` titled `Play back my answers` at `labelLarge` with
the helper `Hear each answer right after you record it.` at `bodyLarge` beneath it, coral
active track, row min height 64, default ON.

Widen the `SessionConfig` built by START SESSION to carry the live values of all six fields
instead of 02-01's hardcoded defaults. Keep the pinned footer, the blocked-helper line and
the Start gate exactly as 02-01 left them.

Every value is `State` field only - no `SharedPreferences`, no database table, no file, no
schema version bump (D-18). Add a class doc comment recording that decision and naming the
deferred alternative (a one-row settings table) so a future reader knows it was considered.
Read every colour and text style through `Theme.of(context)`; never write a hex literal;
never pin `textScaler`.
  </action>
  <acceptance_criteria>
    - `flutter analyze` reports no issues.
    - `grep -c 'ChoiceChip' lib/screens/setup_screen.dart` is at least 1 and the six level labels each appear exactly once.
    - `grep -c 'divisions: 99' lib/screens/setup_screen.dart` equals 1; `grep -c 'divisions: 27'` equals 1; `grep -c 'divisions: 110'` equals 1.
    - `grep -c 'semanticFormatterCallback' lib/screens/setup_screen.dart` equals 3.
    - `grep -c 'SwitchListTile' lib/screens/setup_screen.dart` equals 1.
    - `grep -c 'Color(0x' lib/screens/setup_screen.dart` equals 0.
    - `grep -c 'shared_preferences' pubspec.yaml` equals 0 - no new persistence surface.
    - `flutter test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <done>All six settings are configurable on Setup, match the UI-SPEC control table, and reach the pushed session as one SessionConfig.</done>
</task>

<task type="auto">
  <name>Task 2: Widget tests for defaults, ranges, the Start gate and text-scale containment</name>
  <read_first>
    - `test/screens/history_screen_test.dart` (the widget-test host shape, fake injection and keyed-finder style)
    - `lib/screens/setup_screen.dart` as left by Task 1
    - `.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-UI-SPEC.md` sections "Copywriting Contract → Setup screen copy (complete)" and "Layout Contract → Setup control specifications"
  </read_first>
  <files>test/screens/setup_screen_test.dart</files>
  <behavior>
    - First frame shows the D-16/D-17 defaults: B1 selected, readouts 10, 5 sec and 60 sec, the replay switch on, no topic checked.
    - START SESSION is disabled and the blocked helper is visible while zero topics are checked; checking one enables Start and hides the helper; unchecking the last one restores both.
    - Dragging each slider to its extremes yields exactly 1 and 100, 3 and 30, and 10 and 120 - never a value outside those ranges.
    - Selecting a different CEFR chip deselects the previous one; exactly one chip is selected at all times.
    - The built SessionConfig carries the live values of all six fields.
    - Each slider exposes a semanticFormatterCallback string naming its unit.
    - At a text scale of 2.0 the screen scrolls rather than overflowing, and the pinned footer grows rather than clipping.
    - With an injected empty subject list, the locked empty-state copy renders and Start stays disabled.
  </behavior>
  <action>
Create `test/screens/setup_screen_test.dart` mirroring `lib/` path-for-path. Host `SetupScreen`
inside a `MaterialApp` carrying the app's real `ThemeData` so theme-derived styles resolve.
Inject an ffi-backed `DatabaseHelper` against a temp directory so the orphan sweep in
`initState` runs against a real, empty database rather than a platform channel - reuse the
`sqfliteFfiInit()` / `databaseFactory = databaseFactoryFfi` / temp `documentsDirProvider`
setup from `test/db/database_helper_test.dart`.

Cover the eight behaviours above. Drive sliders with `tester.drag` on the slider finder and
assert the rendered readout text, not internal state. Assert the produced `SessionConfig` by
navigating and reading the pushed route's widget, or by exposing the built config through a
test-visible callback - whichever keeps production code free of test-only branches. For the
text-scale case wrap in `MediaQuery` with `TextScaler.linear(2.0)` and assert
`tester.takeException()` is null (a RenderFlex overflow throws in a test). For the empty-subject
case, inject the empty list through the same seam production uses for `kSubjects` - if no seam
exists, add a `List<String>? subjects` constructor parameter defaulting to `kSubjects` rather
than a test-only global.

Assert every user-facing string against the Copywriting Contract verbatim, including the two
helper lines, the blocked helper and the toggle label.
  </action>
  <acceptance_criteria>
    - `flutter test test/screens/setup_screen_test.dart` exits 0 with at least 8 cases.
    - The test file asserts the exact strings `Pick at least one topic to start.`, `CEFR level of the questions you'll get.`, `How long you get to read the question before recording starts.`, `Recording stops automatically after this long.`, `Play back my answers`, `Hear each answer right after you record it.` and `START SESSION`.
    - A case asserts `10`, `5 sec` and `60 sec` are all present on the first frame.
    - A case asserts `tester.takeException()` is null at `TextScaler.linear(2.0)`.
    - `flutter analyze` reports no issues.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze && flutter test test/screens/setup_screen_test.dart</automated>
  </verify>
  <done>Setup's defaults, ranges, gate, copy and text-scale behaviour are all covered by green host tests.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New files**
- `test/screens/setup_screen_test.dart`

**New/changed public symbols**
- `lib/screens/setup_screen.dart` → `SetupScreen` gains an optional `List<String>? subjects` injection seam (defaulting to `kSubjects`) and the full six-field configuration surface
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **SETUP-02 - edge category `unclassified` (unresolved).** The edge probe could not
   classify "User can select a CEFR level (A1, A2, B1, B2, C1, C2)". Planner assumption: the
   set is closed and single-select, so the only interesting states (always exactly one
   selected, never empty) are authored as E2 truths above. Left unresolved rather than
   auto-backstopped; review manually at UAT.
2. **The level is carried but not yet used to filter.** SETUP-02 delivers selection;
   filtering questions by level is BANK-03 in Phase 3. `SessionConfig.level` exists from day
   one precisely so the Phase 3 Firestore query has a field to read. A reviewer who expects
   the level to change which questions appear in Phase 2 is looking at Phase 3's work.
3. **`d` slider granularity.** The plan takes 1-second divisions (110 steps) to satisfy
   SETUP-05 literally. The UI-SPEC sanctions a 5-second-division fallback if on-device drag
   precision proves painful; if that fallback is taken at UAT, re-verify that the `{n} sec`
   readout format and the default of 60 both still hold.
</flagged_assumptions>

<verification>
- `flutter analyze` reports no issues; `flutter test` exits 0.
- Setup's first frame shows B1, 10, 5 sec, 60 sec, replay on and no topic checked.
- Each slider's extremes match its requirement range exactly.
- The screen scrolls without overflow at a text scale of 2.0.
</verification>

<success_criteria>
- SETUP-02 through SETUP-06 are each satisfied by a control the user can actually operate.
- SETUP-07's gate still holds after the four new control groups are added.
- No new persistence surface exists anywhere in the repo (D-18).
</success_criteria>

<output>
Create `.planning/phases/02-full-timed-practice-session-setup-loop-controls/02-03-SUMMARY.md` when done
</output>
