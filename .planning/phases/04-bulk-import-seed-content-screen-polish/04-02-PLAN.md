---
phase: 04-bulk-import-seed-content-screen-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/screens/import_sheet.dart
  - lib/services/question_importer.dart
  - lib/services/question_bank_writer.dart
  - lib/services/json_file_picker.dart
  - test/screens/import_sheet_test.dart
  - test/services/question_importer_test.dart
  - test/fixtures/import_files.dart
autonomous: true
requirements: [IMPORT-01, IMPORT-02, IMPORT-04]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "IMPORT-04: every row of the file is accounted for — added plus already-in-your-bank plus skipped equals the number of entries in the file's data list — and every skipped row is named by its 1-based file position together with the reason it was skipped."
    - "A commit that fails after an earlier chunk committed renders the partial-write state carrying the exact number of rows that landed, never a success-shaped summary."
    - "A failure that happens before the first commit says in so many words that nothing was imported, so the user can tell it apart from a partial write."
    - "A duplicate row is counted in its own neutral category, is never listed per row, and never carries an error icon or error wording."
    - "A file that could not be opened and a file that opened but is not shaped like a question file get two different messages, and only the second one shows the expected shape."
    - "A file that parses but whose data list is empty gets the app's empty-state treatment — heading plus body, no icon, no red — not its failure treatment."
    - "While the write is in flight the sheet cannot be dismissed by the Done button, a drag, a barrier tap, or the system back gesture."
    - "Retrying after a bank-unreachable failure resumes from the bank read; the user is never asked to find their file a second time."
    - "No text taken from the imported file reaches a widget without being newline-stripped and length-capped: question text renders as exactly one ellipsised line and an echoed level value is truncated to 12 characters."
    - "E2 empty: the sheet's zero-data shape is the idle state, which is not blank — it carries the shape example and the primary action; a file containing zero questions is a different fact and renders the file-empty state."
    - "E2 loading: two captioned busy states — an indeterminate spinner with a checking caption, and a determinate bar with the saving caption and the keep-open line; no busy state is an uncaptioned spinner."
    - "E2 error: four distinct terminal failure surfaces with four distinct keys and four distinct messages; red marks the icon and brown carries every word."
    - "E2 populated: the result state is the only state that grows with data, and the eight states are mutually exclusive by a single state key, so a populated sheet can never render a busy affordance or an error icon beside its summary."
    - "E2 partial: the partial-write state is a first-class terminal state with its own key, its own message, exact counts and a safe re-import recovery — not a degraded result state."
    - "E2 overflow: the sheet is scroll-controlled with a 90%-of-screen-height cap and a single scroll view, and its primary button lives at the bottom of that scrolling column so it stays reachable at any text scale."
    - "E2 zero-one-many: the sheet's own chrome does not vary by count; the only sheet-level consequence of many is height, which the scroll view absorbs."
    - statement: "E2 long-text: the JSON shape example, rendered at Body 16 in a proportional font with soft wrapping, wraps legibly rather than clipping on the narrowest supported device at the largest OS text scale, and its peach card grows with it."
      verification: backstop
    - "E3 empty: the duplicates and skipped lines are zero-suppressed independently, so the summary card's minimum shape is one line; a card with zero lines is unreachable because a zero-question file never reaches a write."
    - "E3 loading: the summary card has no loading state of its own — it is built from an already-complete in-memory report and mounts only on the transition into the result state, so it can never render half-populated."
    - "E3 error: a file-level or bank-level failure never renders a summary card, and the partial-write state carries its counts in its own message rather than reusing the success surface."
    - "E3 populated: at most three lines in fixed order — added, then already-in-your-bank, then skipped — each independently zero-suppressed, so the same fact is always in the same place across imports."
    - "E3 partial: the summary card is not the partial surface; reusing the success-shaped card for a partial write is the specific failure this split prevents."
    - "E3 overflow: the summary card is not height-locked; it grows and the sheet scrolls."
    - "E3 zero-one-many: every summary line is a singular/plural pure function rather than a string interpolation, and the zero case is suppression of the line rather than rendering a zero."
    - "E3 long-text: every summary line is app-authored and bounded by an integer count; no content from the file reaches the summary card."
    - "E4 empty: when nothing was skipped, the section label and the list are both omitted entirely and the summary's skipped line is suppressed — no zero-skipped line, no empty container."
    - "E4 loading: the skip list has no loading state of its own; it is built from an already-complete in-memory report and can never render half-populated."
    - "E4 error: the skip list is the surface for row-level problems only; a file-level or bank-level failure renders no list and no counts, because nothing was examined."
    - "E4 populated: each skip row is a two-line block — reason first, then the offending question truncated to one line — separated by 16px on ivory, with a per-row key carrying the row number, and exempt from the 64px tappable-row floor because the rows are read-only."
    - "E4 partial: a skipped row with no usable content omits its question sub-line entirely rather than rendering an empty one; a row that is not an object at all gets the wrong-shape reason and no sub-line."
    - "E4 overflow: the skip list is a plain column inside the sheet's single scroll view, never a nested scrollable, so a 400-row skip list scrolls rather than clipping."
    - "E4 zero-one-many: zero means no section at all; one means one row under the label with the singular summary line; many means N rows in file order with no cap, no show-more and no grouping."
    - statement: "E4 long-text: a 500-character question renders as exactly one ellipsised line at the largest OS text scale, an embedded newline cannot break the row, and a pathological level value is truncated to 12 characters before it reaches a widget."
      verification: backstop
  artifacts:
    - path: "lib/screens/import_sheet.dart"
      provides: "The complete eight-state sheet plus the full string inventory"
      contains: "importSkipReason"
    - path: "test/screens/import_sheet_test.dart"
      provides: "Widget coverage of all eight sheet states and the blocked mid-write dismissal"
      contains: "import-partial"
  key_links:
    - from: "lib/screens/import_sheet.dart"
      to: "lib/services/question_bank_writer.dart"
      via: "catches the partial-write signal and renders its done/total counts"
      pattern: "ImportPartialWriteException"
    - from: "lib/screens/import_sheet.dart"
      to: "lib/services/json_file_picker.dart"
      via: "catches the unreadable-file signal and routes it to the file-problem state"
      pattern: "ImportFileUnreadableException"
  prohibitions:
    - statement: "MUST NOT frame a duplicate or a skipped row as the user's fault — no wording in the row-level report may call a row invalid, an error, or a failure; each reason describes the row, not the person who wrote it."
      category: values
      requirement_id: IMPORT-04
      status: resolved
      verification: judgment
---

<objective>
Expand the tracer's single happy path into the five different facts this phase owes the
user: what landed, what was already there, which rows need an edit and where they are,
which files cannot be used, and when the bank could not be reached at all.

Purpose: IMPORT-04 forbids a silent or partial failure. This plan is where "reports
success/failure per row" becomes true, and where the one genuinely partial outcome —
a chunk failing after an earlier one committed — becomes a reported state instead of a
mystery.

Output: the complete eight-state import sheet, the full string inventory, the untrusted-
input caps, and widget coverage of every state.
</objective>

<execution_context>
@/Users/tranhao/Desktop/english_practice_app/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranhao/Desktop/english_practice_app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-CONTEXT.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-UI-SPEC.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-PATTERNS.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols added by THIS PLAN (they do not exist before it runs):

| Kind | Symbol |
|------|--------|
| const | `kImportSkipListLabel`, `kImportUnreadableFileMessage`, `kImportBadShapeMessage`, `kImportEmptyFileMessage`, `kImportEmptyFileBody`, `kImportUnreachableMessage`, `kMaxEchoedLevelChars` |
| function | `importDuplicatesLine`, `importSkippedLine`, `importSkipReason`, `importPartialMessage`, `sanitizedEcho` |
| widget key | `import-file-problem`, `import-file-problem-unreadable`, `import-file-problem-shape`, `import-empty-file`, `import-unreachable`, `import-partial`, `import-choose-different-file`, `import-retry`, `import-skip-row-{n}` |

<tasks>

<task type="auto">
  <name>Task 1: Every row the file contained is accounted for on screen</name>
  <read_first>
    - `lib/screens/import_sheet.dart` — the file plan 04-01 created; this task extends its string block and its result state.
    - `lib/services/question_importer.dart` — `ImportPlan`, `ImportSkip`, `ImportSkipReason` as plan 04-01 defined them.
    - `lib/screens/setup_screen.dart` L69-102 — `noQuestionsMessage`: the pure-function-with-branches form, and specifically its third branch, which exists as a LENGTH BOUND on an unbounded input rather than as a shortcut. The echoed-level cap here is the same reasoning.
    - `lib/screens/setup_screen.dart` L1034-1069 — the else-if chain whose branches carry mutually exclusive keys by construction.
    - `.planning/phases/04-bulk-import-seed-content-screen-polish/04-UI-SPEC.md` — "Complete new-string inventory", "Skip-reason copy rules", "Singular / plural is a pure function", "Region B" S4, "Untrusted Input", and the E3/E4 rows of "## UI Considerations".
  </read_first>
  <files>lib/screens/import_sheet.dart</files>
  <action>
Complete the string inventory at the top of `import_sheet.dart` and build the result state
out to its full specified shape. Every string below is a `const` or a pure function in
exactly one place, and none of them may ever carry an exception message, an error code, a
collection name, a document ID or a project ID.

Add to the string block:
- `kImportSkipListLabel` — "What we skipped".
- `String importDuplicatesLine(int n)` — singular "1 was already in your bank" / plural
  "{n} were already in your bank".
- `String importSkippedLine(int n)` — singular "1 row was skipped" / plural
  "{n} rows were skipped".
- `String importPartialMessage(int done, int total)` — "Only some of your questions made it
  — {done} of {total} were saved. Import the same file again and anything already saved will
  be skipped." Both clauses are load-bearing and neither may be softened: the exact count
  is what tells the user how much landed, and the recovery is only TRUE because the bank
  dedupe pass skips whatever already arrived.
- `const int kMaxEchoedLevelChars = 12;` and
  `String sanitizedEcho(String raw, int maxChars)` — the one place file-authored text is
  prepared for a widget: collapse every newline, carriage return and tab to a single space,
  collapse runs of whitespace, trim, then truncate to `maxChars` with an ellipsis when
  longer. Doc-comment it as the untrusted-input boundary: everything inside the picked file
  is data, never instructions, and rendering an unvalidated 5,000-character level string or
  a question containing newlines into a compact list row is a layout-integrity problem
  before it is anything else.
- `String importSkipReason(ImportSkip skip)` — a pure function returning a fixed template
  with the row's 1-based position first, an interpunct separator, then the reason. The four
  branches return exactly these strings, where `{n}` is the 1-based position and `{value}`
  is the user's own level text passed through `sanitizedEcho` with `kMaxEchoedLevelChars`
  first: `Row {n} · no question text` for a blank content; `Row {n} · no topic` for a blank
  subject; `Row {n} · level "{value}" isn't one of A1–C2` for a bad level; and
  `Row {n} · not shaped like a question` for a row that is not an object. Position first,
  always: a reason without a location tells the user a shape of problem, not a place. The
  accepted CEFR set is written as a range because that is what the Setup level chips already
  show. Each branch describes the ROW rather than judging the person who wrote it — that is
  why none of the four uses blame vocabulary, and the doc comment must say so.

Build the result state (`import-result`) to its full shape:
- A PEACH `Material` summary card at radius 24 with 24px padding holding, in FIXED order:
  the added line (always rendered), then the duplicates line, then the skipped line. The
  duplicates and skipped lines are **zero-suppressed independently** — they render only
  when their count is above zero — so a clean import shows exactly one line. The order is
  fixed rather than "whatever is non-zero first" so the same fact is always in the same
  place across imports.
- Below the card, on IVORY with no card of its own (peach never sits on peach), and only
  when the skipped count is above zero: `kImportSkipListLabel` at `labelLarge`, then a plain
  `Column` of skip rows separated by 16px. Each row is a `Key('import-skip-row-{n}')` block
  of the reason line at `bodyLarge`, and — only when the skip carries usable question text —
  a second line rendering that text through `sanitizedEcho` with `maxLines: 1` and ellipsis
  overflow. A row with no usable content omits the sub-line entirely rather than rendering
  an empty one. Rows are NOT wrapped in a 64px-minimum constraint: that floor exists for
  tappable rows, and these are read-only static content.
- The skip list is a plain `Column` inside the sheet's single existing `SingleChildScrollView`
   — never a nested scrollable, because a nested scrollable inside a draggable sheet is a
  gesture conflict. There is no cap, no show-more and no grouping: rows are in file order so
  the list reads in the same order as the user's editor.
- Duplicates are **never enumerated per row**. Enumerating 400 duplicate rows would bury the
  three rows that actually need an edit, which is the opposite of what the skip list is for.
- Then the 24px gap and the existing Done button.

Every colour and text style still comes from `Theme.of(context)`; no icon and no error red
appears anywhere in this state, because a duplicate is a neutral fact and a skipped row is
"your file needs an edit", not "something went wrong".
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos &amp;&amp; flutter test test/services/question_importer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze --no-fatal-infos` exits 0.
    - `grep -q "kMaxEchoedLevelChars = 12" lib/screens/import_sheet.dart` succeeds.
    - `grep -q "String importSkipReason" lib/screens/import_sheet.dart`, `grep -q "String importDuplicatesLine" lib/screens/import_sheet.dart`, `grep -q "String importSkippedLine" lib/screens/import_sheet.dart` and `grep -q "String importPartialMessage" lib/screens/import_sheet.dart` all succeed.
    - `grep -q "import-skip-row-" lib/screens/import_sheet.dart` succeeds.
    - `grep -q "no question text" lib/screens/import_sheet.dart`, `grep -q "no topic" lib/screens/import_sheet.dart`, `grep -q "not shaped like a question" lib/screens/import_sheet.dart` and `grep -q "one of A1" lib/screens/import_sheet.dart` all succeed — the four skip-reason branches return the specified copy verbatim.
    - `grep -q "maxLines: 1" lib/screens/import_sheet.dart` succeeds and appears on the skip-row question text.
    - No hex colour literal in the file: `grep -c "0xFF" lib/screens/import_sheet.dart` returns 0.
  </acceptance_criteria>
  <done>A result with 85 added, 12 duplicates and 3 skips renders one peach card of three lines plus three located, reasoned, length-capped skip rows; a clean result renders one line and a Done button.</done>
</task>

<task type="auto">
  <name>Task 2: Four terminal failures, four surfaces, and a write that cannot be walked out of</name>
  <read_first>
    - `lib/screens/import_sheet.dart` — the state enum and dispatch chain to extend.
    - `lib/services/json_file_picker.dart` and `lib/services/question_bank_writer.dart` — the signal exceptions the sheet must catch and route.
    - `lib/services/question_importer.dart` — the file-shape and file-empty signals.
    - `lib/screens/practice_screen.dart` L280-289 — the `PopScope` usage that is correct for the pinned Flutter version, already used to route the back gesture into the Stop dialog.
    - `lib/screens/setup_screen.dart` L242-274 — the two-arm catch (the seam's own signal first, then a bare catch for a source misbehaving, both landing on the SAME honest state) and the console-only failure detail comment; L746-784 — `_TopicsError`'s 48px red icon / 16px / centred brown message geometry.
    - `.planning/phases/04-bulk-import-seed-content-screen-polish/04-UI-SPEC.md` — "Region B" S5/S6/S7/S8, "Interaction Contract", "Why 'nothing was imported' and 'anything already saved will be skipped' are load-bearing", and the E2 rows of "## UI Considerations".
  </read_first>
  <files>lib/screens/import_sheet.dart, lib/services/json_file_picker.dart, lib/services/question_bank_writer.dart</files>
  <action>
Replace plan 04-01's provisional "catch, log, land back on idle" with the four terminal
states, and block dismissal for exactly the one state where leaving costs the user the
truth.

Add to the string block:
- `kImportUnreadableFileMessage` — "Couldn't open that file — try picking it again."
- `kImportBadShapeMessage` — "That file isn't shaped like a question file — it needs to look
  like this:"
- `kImportEmptyFileMessage` — "That file has no questions in it." and
  `kImportEmptyFileBody` — "Pick a file whose data list has at least one question in it."
- `kImportUnreachableMessage` — "Couldn't reach your question bank — nothing was imported.
  Check your connection and try again." The middle clause is load-bearing: the bank read
  gates the whole import, so when it fails not one document has been written, and without
  that clause the user cannot tell this state from a partial write.

Each constant gets the doc-comment structure `kTopicsErrorMessage` and
`kQuestionLoadErrorMessage` model at `setup_screen.dart` L28-67: why it is a sibling rather
than the same string, and what containment rule it obeys. In particular say why the
unreadable-file message and the bad-shape message are two strings and not one: showing the
expected shape for a file that never opened would be a false explanation, because the app
would be blaming the content of something it never read.

Extend the state enum and the dispatch chain with four new branches, keeping every branch's
key mutually exclusive by construction:

- **`import-file-problem`** — reached from the picker's unreadable signal and from the
  importer's file-shape signal. A 48px error-red `Icons.error_outline_rounded`, 16px, then
  the message in BROWN and centred, then — only on the shape branch — the SAME shape-example
  widget and the SAME constant the idle state uses (one shape string, two render sites), then
  the 64px full-width primary button labelled "Choose a different file" with key
  `import-choose-different-file`. The two branches carry sub-keys
  `import-file-problem-unreadable` and `import-file-problem-shape` so each is separately
  assertable.
- **`import-empty-file`** — reached from the importer's file-empty signal. **No icon and no
  red**: this is a file that is empty, not wrong, so it gets the app's empty-state treatment
  — the heading at `headlineSmall`, 8px, the body at `bodyLarge`, both centred — then the
  same "Choose a different file" primary button.
- **`import-unreachable`** — reached when the bank read fails, and when a write fails before
  any chunk has committed. Error-red 48px icon, 16px, the message in brown, then a 64px
  full-width primary `FilledButton` labelled "Try again" with key `import-retry`. It is a
  filled button here where the same label is a text button on the Setup topics card,
  because in the sheet it is the only action present. **Retry resumes from the bank read**,
  not from the picker: the file has already been read and parsed successfully, so a briefly
  offline user must not have to hunt for their file a second time. Hold the parsed result in
  state so the retry has something to resume with.
- **`import-partial`** — reached from the writer's partial-write signal. Error-red 48px icon,
  16px, `importPartialMessage(done, total)` in brown and centred, then a 64px full-width
  primary button "Choose the file again" and, 8px below it, a `TextButton` "Done" at
  `minimumSize: Size(64, 48)` with key `import-done`. Two buttons is a deliberate exception:
  Done is still the single EXIT, and "Choose the file again" restarts the flow inside the
  sheet exactly as "Choose a different file" already does.

Wire the catches with the two-arm pattern `_loadSubjects` models: the seam's own signal
exception first, then a bare `catch (error, stack)` with `debugPrintStack` for a seam
misbehaving, landing on the SAME honest state. Exception detail goes to `debugPrint` and
nowhere else.

Block dismissal in exactly one state:
- Wrap the sheet body in `PopScope` with `canPop` false while the writing state is active,
  using the same current API `practice_screen.dart` already uses. The write is short and its
  caption already says the keep-open line, so a blocked gesture is explained before it is
  attempted rather than after.
- **Backstop:** whether drag-to-dismiss and barrier taps route through `PopScope` — as
  opposed to only the system back gesture — is Flutter-version-dependent. Verify all three
  routes on the pinned version. If drag or barrier bypasses it, the sanctioned fallback is to
  present the sheet as non-dismissible and non-draggable for the write phase — **never** a
  silently-ignored gesture. Record which of the two shipped in the plan summary.

Double-action prevention is unchanged and structural: the state that owns each button is
replaced the instant it is tapped, so there is never a frame in which the same action can
fire twice. No button anywhere in the sheet swallows a tap; a disabled button passes a null
handler.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze --no-fatal-infos` exits 0 and `flutter test` exits 0.
    - `grep -q "PopScope" lib/screens/import_sheet.dart` succeeds and the file does not use the deprecated pop callback: `grep -c "onPopInvoked:" lib/screens/import_sheet.dart` returns 0.
    - All six new keys are present: `for k in import-file-problem import-file-problem-unreadable import-file-problem-shape import-empty-file import-unreachable import-partial; do grep -q "$k" lib/screens/import_sheet.dart || exit 1; done`.
    - `grep -q "nothing was imported" lib/screens/import_sheet.dart` succeeds — the bank-unreachable copy states that no document was written.
    - The bank-unreachable and partial messages are the only two places `Icons.error_outline_rounded` appears alongside the file-problem state, and the file-empty state contains no error icon — assert by reading the empty-file branch.
    - The shape-example constant is referenced from at least two render sites: `grep -c "kImportShapeExample" lib/screens/import_sheet.dart` returns at least 3 (its declaration plus two uses).
    - `grep -c "0xFF" lib/screens/import_sheet.dart` returns 0.
  </acceptance_criteria>
  <done>Each of the four terminal failures renders its own keyed surface with its own message and its own affordance, and the sheet refuses every dismissal route while a write is in flight.</done>
</task>

<task type="auto">
  <name>Task 3: Widget coverage for all eight states</name>
  <read_first>
    - `test/screens/import_sheet_test.dart` — the file plan 04-01 created, with `FakeJsonFilePicker` and `FakeQuestionBankWriter`.
    - `test/screens/setup_screen_test.dart` L23-100 — the scripted-fake contract these doubles follow, the synchronous-completion rule, and the `Completer` hold gate that makes an in-flight frame assertable.
    - `test/fixtures/import_files.dart` — extend it rather than adding a second fixture file.
    - `lib/screens/import_sheet.dart` — the finished state machine under test.
  </read_first>
  <files>test/screens/import_sheet_test.dart, test/fixtures/import_files.dart</files>
  <action>
Cover every state and every transition with `testWidgets`, driving both platform seams
through the fakes. Group per state, and give each test a name that states the fact it pins.

- **Idle.** Opening the sheet renders the idle key, the shape example and the choose-file
  button. Cancelling the picker returns to idle and renders NO message of any kind —
  cancellation is not a failure.
- **Checking.** With the writer's bank read parked on its hold gate, the checking key and
  the bank-comparison caption are both on screen and no button is; releasing the gate moves
  on. Assert the bank read is called exactly once per import run via the fake's counter.
- **Writing.** With the write parked, the writing key, the saving caption, the keep-open
  line and a determinate progress bar are on screen; the progress value reflects only
  committed chunks. Assert the sheet cannot be popped in this state and CAN be popped in the
  result state.
- **Result — clean.** A file whose rows are all new renders exactly one summary line and no
  skip section and no duplicates line.
- **Result — mixed.** A file with valid rows, duplicate rows and skipped rows renders all
  three summary lines in fixed order, the skip section label, and one keyed row per skip
  whose reason names the correct 1-based position. Assert no duplicate is listed per row.
- **Result — untrusted content.** A skip whose level value is 200 characters long renders a
  reason line no longer than the template plus the 12-character cap plus an ellipsis, and a
  skip whose question text contains newlines renders one line. Assert the raw newline
  characters are absent from the rendered text.
- **File problem.** Two separate tests: the picker's unreadable signal renders the
  unreadable sub-key and NO shape example; the importer's file-shape signal renders the
  shape sub-key WITH the shape example.
- **File empty.** A file whose data list is empty renders the empty-file key, and asserts no
  error icon is present in that subtree.
- **Bank unreachable.** The bank read throwing renders the unreachable key; tapping the
  retry key calls the bank read a second time and does NOT call the picker again.
- **Partial write.** The writer throwing the partial signal after a first chunk renders the
  partial key with both counts in the message, offers the re-choose button, and its Done
  button still closes the sheet.
- **Every failure preserves the user's setup.** After each failure state, pop the sheet and
  assert Setup's selected topics, level and slider values are unchanged.

Extend `test/fixtures/import_files.dart` with the raw JSON strings these cases need,
including a file with more than `kMaxWritesPerBatch` rows so the multi-chunk progress path
is exercised on the host, keeping the `kFixture` prefix and a one-line comment on each
naming the rule it exercises.
  </action>
  <verify>
    <automated>flutter test test/screens/import_sheet_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/screens/import_sheet_test.dart` exits 0 and `flutter test` exits 0.
    - `grep -c "testWidgets(" test/screens/import_sheet_test.dart` returns at least 14.
    - Every one of the eight state keys is asserted somewhere in the file: `for k in import-idle import-checking import-writing import-result import-file-problem import-empty-file import-unreachable import-partial; do grep -q "$k" test/screens/import_sheet_test.dart || exit 1; done`.
    - The multi-chunk case exists: `grep -q "kMaxWritesPerBatch" test/screens/import_sheet_test.dart` succeeds.
    - The retry-does-not-re-pick assertion exists: the test asserting the retry key also asserts the picker's call count is still 1.
  </acceptance_criteria>
  <done>All eight sheet states, the blocked mid-write dismissal, the retry-resumes-from-the-bank-read rule and the untrusted-input caps are covered by host tests that need no device.</done>
</task>

</tasks>

<verification>
- `flutter analyze --no-fatal-infos` exits 0 and `flutter test` exits 0.
- `! grep -REq 'package:cloud_firestore|package:file_picker' lib/screens/` — still no vendor
  package under `lib/screens/`.
- `grep -c "0xFF" lib/screens/import_sheet.dart` returns 0 — every colour comes from the theme.
- The sheet's state keys are mutually exclusive: no test can find two of the eight keys in
  the same pumped frame.
</verification>

<success_criteria>
An import of a file with 85 new rows, 12 rows already in the bank and 3 bad rows reports
"85 questions added", "12 were already in your bank", "3 rows were skipped" and names each
skipped row by its position in the file and why — and every way the import can fail
(unreadable file, wrong shape, empty file, unreachable bank, partial write) has its own
message, its own affordance and its own key.
</success_criteria>

## Flagged assumptions

1. **Backgrounding the app mid-write is UNRESOLVED** (carried verbatim from the UI-SPEC's
   Interaction Contract). The assumption is that the commit continues — a Firestore commit
   is not tied to the widget tree and a bottom sheet is not disposed by backgrounding — and
   that the sheet still shows its outcome on return. Task 3 cannot prove this on the host;
   it must become an explicit UAT case in plan 04-05. If the OS kills the app mid-write the
   outcome is indistinguishable from a partial write with no one to report it, and the
   recovery is unchanged and still safe, because re-importing the same file skips whatever
   landed.
2. **Whether drag-to-dismiss and barrier taps route through `PopScope`** on the pinned
   Flutter version is unverified in this repo. Task 2 names the sanctioned fallback and
   requires the shipped choice to be recorded in the summary.
3. **`file_picker ^11.0.3`'s result shape** (path versus bytes, per platform) and any
   Android/iOS configuration it needs beyond the pubspec entry are unverified — no research
   was run for this phase. The picker seam handles both result shapes; the manifest
   consequence is audited in plan 04-05.

<output>
Create `.planning/phases/04-bulk-import-seed-content-screen-polish/04-02-SUMMARY.md` when done
</output>

