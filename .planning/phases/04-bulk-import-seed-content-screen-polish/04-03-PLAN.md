---
phase: 04-bulk-import-seed-content-screen-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/services/firestore_question_source.dart
  - lib/screens/setup_screen.dart
  - docs/NAVIGATION.md
  - test/screens/setup_screen_test.dart
  - test/services/firestore_question_source_test.dart
autonomous: true
requirements: [UI-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "UI-03: `docs/NAVIGATION.md` names every navigable surface in the app together with its classification, and exactly three of them are classified as core screens — Setup, Practice and History."
    - "A reader who counts four files under `lib/screens/` can find, in the repo, the written rule that says why that is three core screens and not four."
    - "Selecting more than 30 topics and tapping Start shows a message that names the real cause and the real fix, and never the connection."
    - "The over-limit guard throws a type distinct from the bank-unreachable signal, so the Start handler routes the two causes to two different messages."
    - "At most one of the four Start-footer helper keys is present in any frame; a run that lands on the over-limit helper never produced a result or a network failure."
    - "The over-limit helper clears on any topic toggle, any level change, any slider change and the next Start tap, exactly as the other Start helpers do."
    - "Every topic, the level and all three slider values survive the over-limit path unchanged."
    - "E5 empty: the nothing-selected helper's Phase 3 trigger is unchanged and cannot coexist with the over-limit helper, because reaching the guard requires at least 31 topics checked."
    - "E5 loading: unchanged — the busy Start button owns the in-flight state, and the over-limit guard throws before the query is issued, so the busy state is measured in frames rather than round trips."
    - "E5 error: the over-limit helper renders in brown Body with NO icon, under its own key, and replaces the connection-blaming message for this one cause."
    - "E5 partial: unchanged — only topic selection gates Start; the level and all three sliders always hold values."
    - "E5 overflow: the non-scrolling footer's maximum height is unchanged this phase — the new helper is the fifth option in the same single mutually-exclusive slot, is shorter than the existing failure message, and carries no icon where that one carries a 24px icon."
    - "E5 long-text: the over-limit helper is a fixed string shorter than the failure message the non-scrolling footer already accommodates, and it has the full footer width to wrap into because it carries no icon."
    - "E6 empty: a bank that was empty before an import goes from the no-topics state to the populated state silently via the existing background refresh; the empty copy already names importing as the unlock, and this phase is the first time that sentence describes an affordance the user can see."
    - "E6 populated: the topic row count is data-driven and grows by whatever the import added, with no new visual treatment, no badge and no diff highlight — the sheet already reported the counts."
    - "E6 zero-one-many: unchanged from Phase 3; the newly reachable jump from zero rows to many in a single frame is absorbed because the topics card is not height-locked."
    - statement: "E6 long-text: a deliberately long imported subject name wraps and grows its 64px topic row rather than clipping at the largest OS text scale — this must be exercised with a long imported subject, not assumed from the ten curated seed names."
      verification: backstop
  artifacts:
    - path: "docs/NAVIGATION.md"
      provides: "The written route + surface inventory that discharges UI-03 (D-49)"
      contains: "Core screen"
    - path: "lib/services/firestore_question_source.dart"
      provides: "TooManyTopicsException and the corrected kMaxTopicsPerQuery doc comment"
      contains: "class TooManyTopicsException"
    - path: "lib/screens/setup_screen.dart"
      provides: "kTooManyTopicsMessage and the fourth Start-footer helper branch"
      contains: "kTooManyTopicsMessage"
  key_links:
    - from: "lib/screens/setup_screen.dart"
      to: "lib/services/firestore_question_source.dart"
      via: "_startSession catches the over-limit signal separately from the unavailable signal"
      pattern: "TooManyTopicsException"
    - from: "docs/NAVIGATION.md"
      to: "lib/screens/"
      via: "names every screen file and every modal surface with its classification"
      pattern: "SessionDetailScreen"
---

<objective>
Make UI-03 checkable instead of arguable, and stop the app blaming the user's connection
for a limit that has nothing to do with the network.

Purpose: `lib/screens/` holds four files and UI-03 says three, so the requirement cannot be
ticked without a written rule for what it counts (D-49). Separately, the over-30-topics
guard currently lands on the generic Start-failure message; the importer this phase ships is
exactly the thing that can make that branch reachable, so the accepted imprecision expires
here (D-61).

Output: `docs/NAVIGATION.md`, a distinct over-limit exception type, the fourth Start-footer
helper, and the corrected doc comments.
</objective>

<execution_context>
@/Users/tranhao/Desktop/english_practice_app/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranhao/Desktop/english_practice_app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-CONTEXT.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-UI-SPEC.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-PATTERNS.md
@.planning/phases/04-bulk-import-seed-content-screen-polish/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and paths created by THIS PLAN:

| Kind | Symbol / path |
|------|---------------|
| file | `docs/NAVIGATION.md` |
| exception | `TooManyTopicsException` |
| const | `kTooManyTopicsMessage` |
| enum | `StartMessageKind` |
| widget key | `setup-start-too-many-topics` |

<tasks>

<task type="auto">
  <name>Task 1: The over-limit refusal gets its own type and its own sentence</name>
  <read_first>
    - `lib/services/firestore_question_source.dart` — the whole file. `kMaxTopicsPerQuery`'s doc comment at L43-84 is the one comment this phase must EDIT, `QuestionBankUnavailableException` at L25-41 is the exception shape to copy, and the guard at L218-228 is the throw site.
    - `lib/screens/setup_screen.dart` — L28-67 (`kTopicsErrorMessage` and `kQuestionLoadErrorMessage` and their why-this-is-a-sibling doc comments), L193-207 (`_startMessage` and `_startMessageIsFailure`, and the comment explaining that ONE message plus a treatment flag is what makes the helpers mutually exclusive by construction), L345-358 (`_clearStartMessage` and its call sites), L366-455 (`_startSession` and the catch at L395-406), L584-601 (`_StartFooter`'s construction), L993-1069 (`_StartFooter`'s fields and `_helper`'s else-if chain).
    - `.planning/phases/04-bulk-import-seed-content-screen-polish/04-UI-SPEC.md` — "Region C", the B5 row, "The >30-topics helper deliberately gets no icon", and the E5 rows of "## UI Considerations".
  </read_first>
  <files>lib/services/firestore_question_source.dart, lib/screens/setup_screen.dart</files>
  <action>
Split the over-limit refusal away from the bank-unreachable signal, all the way from the
throw to the sentence on screen.

In `lib/services/firestore_question_source.dart`:
- Add `class TooManyTopicsException implements Exception` beside
  `QuestionBankUnavailableException`, with the same shape: `const` constructor, a
  `toString()` returning its own type name, and a doc comment saying it is a developer-facing
  *signal* that is never rendered, and stating what makes it a sibling rather than the same
  type — the two failures have two different causes and two different fixes, and only a
  distinct type lets the Start handler tell them apart.
- Change the guard in `questionsFor` so it throws the new type instead of
  `QuestionBankUnavailableException`. The `debugPrint` there stays as-is: it already names
  the count, the limit and the reason for failing loudly rather than trimming.
- Rewrite the tail of `kMaxTopicsPerQuery`'s doc comment. The paragraph that defers the
  batched multi-query merge and its reversal trigger STAYS — the merge is still deferred.
  What must go is the closing claim about the generic Start-failure copy: that copy is no
  longer what this branch lands on, and the acceptance it recorded — granted on the grounds
  that the branch could not be reached at the bank size of the time — is spent, because this
  phase's importer is the thing that can make the branch reachable and this phase's seed
  makes the bank ten subjects wide. Replace that closing paragraph with a statement that the
  branch now throws its own type, that Setup renders its own fixed string for it, and that
  the batching itself remains deferred with its trigger unchanged. Tag the change D-61.

In `lib/screens/setup_screen.dart`:
- Add `const String kTooManyTopicsMessage = 'You can drill at most 30 topics at once — '
  'uncheck a few.';` beside the other two failure constants, with a doc comment in their
  form: why it is a sibling and not the same string (it names a limit the user can satisfy
  by unchecking a topic that is visible on the same screen, where the other two name a
  connection), and what containment rule it obeys (no exception text, no error code, no
  collection name, no document ID, no project ID).
- **Do not add a second boolean.** <!-- planner-discipline-allow: _startMessageIsFailure -->
  The existing `_startMessageIsFailure` field is a single flag precisely so the helper
  branches are mutually exclusive by construction; a second bool
  could be true at the same time as the first. Replace it with a small private enum —
  `StartMessageKind` with values for the zero-result case, the bank-failure case and the
  over-limit case — carried alongside `_startMessage` and assigned together with it in
  `_startSession` and `_clearStartMessage` and nowhere else. Update `_StartFooter`'s field
  and its call site accordingly, and update the doc comment at L199-207 so it explains the
  enum's totality rather than the retired bool.
- In `_startSession`, add a catch arm for the new type BEFORE the existing
  `QuestionBankUnavailableException` arm, setting `_startMessage` to `kTooManyTopicsMessage`
  and the kind to the over-limit value. Everything else about the failure path is unchanged:
  `_starting` goes false, the detail goes to `debugPrint` only, and every topic, the level
  and all three slider values survive.
- In `_StartFooter._helper`, add the fourth branch to the else-if chain, keyed
  `setup-start-too-many-topics`, rendering the message centred at `bodyLarge` in the default
  brown with **no icon**. The absence of an icon is deliberate and load-bearing: this is not
  a fault, it is a limit the user can satisfy, and giving it the red error icon would file it
  under "something went wrong, retry" — the exact misreading this string exists to end. Keep
  the branch order such that the four keys stay mutually exclusive by construction rather
  than by conditions kept disjoint by hand.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos &amp;&amp; flutter test test/services/firestore_question_source_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze --no-fatal-infos` exits 0.
    - `grep -q "class TooManyTopicsException" lib/services/firestore_question_source.dart` succeeds.
    - `grep -q "throw const TooManyTopicsException" lib/services/firestore_question_source.dart` succeeds — the over-limit guard throws the new type.
    - `flutter test test/screens/setup_screen_test.dart` includes a case proving a Start attempt that throws the over-limit type renders a different helper key from one that throws the bank-unavailable type.
    - `grep -q "kTooManyTopicsMessage" lib/screens/setup_screen.dart` succeeds and the constant's value contains the phrase `at most 30 topics at once`.
    - `grep -q "setup-start-too-many-topics" lib/screens/setup_screen.dart` succeeds.
    - `grep -c "_startMessageIsFailure" lib/screens/setup_screen.dart` returns 0 — the bool is replaced, not supplemented.
    - `grep -q "enum StartMessageKind" lib/screens/setup_screen.dart` succeeds.
    - `grep -q "D-61" lib/services/firestore_question_source.dart` succeeds — the `kMaxTopicsPerQuery` comment change is tagged with its decision ID.
    - `grep -c "unreachable at the current bank size" lib/services/firestore_question_source.dart` returns 0.
    - The over-limit helper branch contains no `Icon(` call.
  </acceptance_criteria>
  <done>A Start attempt with 31 or more topics selected shows a message naming the limit and the fix, under its own key, with no error icon and with every setting intact.</done>
</task>

<task type="auto">
  <name>Task 2: Write down what counts as a screen, so UI-03 can be counted</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — UI-03's exact wording is what this document must discharge.
    - `.planning/phases/04-bulk-import-seed-content-screen-polish/04-UI-SPEC.md` — the "Navigation Contract (UI-03, D-49)" section: the three-clause definition of a core screen and the complete surface inventory table. This document is the repo-side home of that contract; it must not invent a different rule.
    - `lib/screens/setup_screen.dart` L324-332 and the new `_openImportSheet` — the two ways off Setup, one a route push and one a modal.
    - `lib/screens/history_screen.dart` and `lib/screens/session_detail_screen.dart` — confirm how the detail view is reached before classifying it.
    - `lib/screens/practice_screen.dart` — confirm the Stop-confirmation dialog is a modal, not a route.
    - `lib/main.dart` — confirm which screen is `MaterialApp.home`.
  </read_first>
  <files>docs/NAVIGATION.md</files>
  <action>
Create `docs/NAVIGATION.md` as the repo's navigation contract — the artifact that makes
UI-03 checkable by a future reader who counts four files under `lib/screens/` and wants to
know whether that is a bug.

It must contain, in this order:

1. **A one-paragraph statement of the problem it solves**: UI-03 says exactly three core
   screens with no extraneous navigation; the codebase has four screen files and two modal
   surfaces; the requirement cannot be ticked off without a written rule for what it counts.
2. **The definition of a core screen**, as three numbered clauses joined by AND, taken
   verbatim in substance from the UI-SPEC: the user navigates TO it from the app's own
   chrome (an app-bar action or a primary call to action); it owns a distinct task the user
   can stay in indefinitely; and it is reachable without first opening another destination's
   content. State explicitly that a drill-down of a list item, a modal sheet and a modal
   dialog each fail at least one clause and are therefore not core screens.
3. **The complete surface inventory as a table** with the columns Surface, Reached from,
   Pushes a route?, and Classification. Rows, with the classification given:
   `SetupScreen` (the app's home, root, core screen 1 of 3); `PracticeScreen` (from Setup's
   primary call to action, pushes a route, core screen 2 of 3); `HistoryScreen` (from
   Setup's app-bar history action, pushes a route, core screen 3 of 3);
   `SessionDetailScreen` (from tapping a row inside History, pushes a route, DETAIL VIEW —
   it fails clause 3 because it is unreachable without opening a History row, and clause 1
   because it is reached from content rather than from chrome); the import bottom sheet
   (from Setup's app-bar import action, pushes NO route, MODAL — it fails clause 2 because
   it is a transient maintenance task with a single terminal outcome); the stop-confirmation
   dialog (from the Practice app bar or the back gesture, no route, MODAL); snackbars and
   banners (in place, no route, IN-PLACE FEEDBACK — not destinations).
4. **The result line**: exactly 3 core screens (Setup, Practice, History), 1 detail view,
   2 modals, 0 extraneous navigation — UI-03 holds.
5. **The rule that keeps it holding**: any future surface that would push a page route from
   app-bar chrome or from a primary call to action becomes a fourth core screen and must
   either justify itself against UI-03 or be built as a modal or a detail view. State that
   this is exactly why the importer was built as a bottom sheet: a full-screen pushed route
   would have made the route count five in the one phase whose requirement is exactly three
   screens, so the screen count is unchanged by construction rather than by argument.
6. **A maintenance note** naming the two places that must change together when a surface is
   added or removed: this file and `.planning/REQUIREMENTS.md`'s UI-03 row.

Link to it from `docs/QUESTION_GENERATION_PROMPT.md`? No — the two documents are unrelated;
leave both standing alone.
  </action>
  <verify>
    <automated>test -f docs/NAVIGATION.md &amp;&amp; grep -c 'Core screen' docs/NAVIGATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/NAVIGATION.md` exists.
    - `grep -c 'Core screen' docs/NAVIGATION.md` returns exactly 3.
    - Every screen class is named in the document: `for c in SetupScreen PracticeScreen HistoryScreen SessionDetailScreen; do grep -q "$c" docs/NAVIGATION.md || exit 1; done` exits 0.
    - `grep -q 'Detail view' docs/NAVIGATION.md` and `grep -qi 'modal' docs/NAVIGATION.md` both succeed.
    - The document contains the three numbered clauses of the core-screen definition and the closing rule for future surfaces.
  </acceptance_criteria>
  <done>A reader can open one repo file, read what a core screen is, count the app's surfaces against that rule, and confirm the answer is three.</done>
</task>

<task type="auto">
  <name>Task 3: Test the Setup surface this phase changed</name>
  <read_first>
    - `test/screens/setup_screen_test.dart` — the whole file's fake and its existing Start-footer tests; add to it rather than starting a second Setup test file.
    - `test/services/firestore_question_source_test.dart` — the pin-the-constant group and the header's statement of what is deliberately not covered.
    - `lib/screens/setup_screen.dart` and `lib/services/firestore_question_source.dart` — as Task 1 left them.
    - `test/fixtures/questions.dart` — the shared subject fixtures these tests drive.
  </read_first>
  <files>test/screens/setup_screen_test.dart, test/services/firestore_question_source_test.dart</files>
  <action>
Extend the two existing test files. Every double is imported, never re-declared — a second
copy of a fake is a second model of the rule it stands for.

In `test/screens/setup_screen_test.dart`:
- **The AppBar has two actions and the import one comes first.** Assert both keys are
  present and that the import action's position in the rendered `actions:` list precedes the
  history action's, so adding an action did not move the one the user's thumb already knows.
- **Opening and dismissing the sheet re-reads the topics.** Tap the import key, dismiss the
  sheet (through the Done path in a fake-driven run), and assert the fake question source's
  subjects call count went up by exactly one. Then assert the same is true for a dismissal
  that does NOT go through Done — pop the modal route directly — because the refresh is
  wired to the sheet's await, not to a button handler.
- **Dismissing the sheet clears a stale Start helper.** Drive a Start that lands on the
  zero-result helper, then open and dismiss the import sheet, and assert the helper key is
  gone: a "no questions in this topic yet" line is very likely to have just been made false
  by the import that closed.
- **The over-limit helper.** Script the fake source's Start query to throw the over-limit
  type, tap Start, and assert `setup-start-too-many-topics` is present, that the message
  names the limit, that the bank-failure helper key is ABSENT, and that no error icon is
  rendered in the footer.
- **Helper totality.** In the same pumped frame, assert that at most one of the four helper
  keys is ever found. Write it as a loop over the four keys asserting the count of found
  widgets across them is not greater than one, so a future fifth helper cannot silently
  break the invariant.
- **The over-limit helper clears.** After it renders, toggle a topic and assert it is gone;
  repeat for a level change and for a slider change.
- **The setup survives.** After the over-limit path, assert the previously selected topics,
  the chosen level and all three slider readouts are unchanged.
- **A long imported subject name.** Drive the fake source with a subject name long enough to
  wrap at the largest text scale and assert the topic row grows rather than throwing a
  layout overflow. This is the E6 backstop and it must be exercised with a deliberately long
  subject, not assumed from the curated seed names.

In `test/services/firestore_question_source_test.dart`:
- Add a group for the new exception type asserting its `toString()` is its own type name and
  that it is a distinct type from the bank-unavailable signal — the whole point of the split
  is that a `catch` can tell them apart.
- Keep the existing `kMaxTopicsPerQuery` pin and extend its comment to record that the
  branch it guards is now routed to its own user-facing string.
  </action>
  <verify>
    <automated>flutter test test/screens/setup_screen_test.dart test/services/firestore_question_source_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` exits 0.
    - `grep -q "setup-start-too-many-topics" test/screens/setup_screen_test.dart` succeeds.
    - `grep -q "setup-import" test/screens/setup_screen_test.dart` succeeds.
    - `grep -q "TooManyTopicsException" test/services/firestore_question_source_test.dart` succeeds.
    - The helper-totality test exists and iterates over all four helper keys rather than asserting on one.
    - `grep -c "class FakeQuestionSource" test/screens/setup_screen_test.dart` returns 1 — no second copy of the double was introduced.
  </acceptance_criteria>
  <done>Every Setup-side change this phase makes — the second AppBar action, the sheet-dismiss refresh, the stale-helper clear, the fourth helper and its exclusivity — is pinned by a host test.</done>
</task>

</tasks>

<verification>
- `flutter analyze --no-fatal-infos` exits 0 and `flutter test` exits 0.
- `ls lib/screens/*.dart | wc -l` returns 5 (the four Phase 1-3 screens plus the import
  sheet, which pushes no route) and `docs/NAVIGATION.md` classifies every one of them.
- `grep -rn "MaterialPageRoute" lib/screens/` lists only the pre-existing push sites — the
  import surface added none.
</verification>

<success_criteria>
UI-03 can be checked off by reading one repo document and counting, and a user who selects
more than thirty topics is told the real cause and the real fix instead of being told to
check their connection.
</success_criteria>

<output>
Create `.planning/phases/04-bulk-import-seed-content-screen-polish/04-03-SUMMARY.md` when done
</output>
