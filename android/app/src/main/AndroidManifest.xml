<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by package:record to capture spoken practice answers.
         Recordings are written to app-private storage only — no
         WRITE_EXTERNAL_STORAGE is requested, and recordings never leave the
         device.

         RECORD_AUDIO is still the ONLY permission declared in this file. From
         Phase 3 the merged release manifest also carries INTERNET, contributed
         TRANSITIVELY by cloud_firestore so the app can read the Firestore
         question bank (D-39) — it is deliberately not declared here, because
         declaring it would hide which dependency actually asks for it.

         That claim is verified against the MERGED RELEASE manifest
         (build/app/intermediates/merged_manifest/release/.../AndroidManifest.xml),
         not against this source file: a source file cannot tell you what a
         dependency added. Re-run that inspection whenever a dependency changes,
         and confirm INTERNET is the only thing Firebase brought — no analytics,
         no ads, no surprise transitive permission. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:label="englishreflex"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
