<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by package:record to capture spoken practice answers.
         Recordings are written to app-private storage only — no
         WRITE_EXTERNAL_STORAGE is requested, and recordings never leave the
         device.

         RECORD_AUDIO is still the ONLY permission declared in this file. From
         Phase 3 the merged release manifest also carries INTERNET *and*
         ACCESS_NETWORK_STATE — BOTH contributed transitively by
         firebase-firestore, so the app can read the Firestore question bank and
         so the SDK can observe connectivity and decide when to fall back to its
         on-device cache (D-39). Neither is declared here, deliberately:
         declaring them would hide which dependency actually asks for them.

         That claim is verified against the MERGED RELEASE manifest
         (build/app/intermediates/merged_manifest/release/processReleaseMainManifest/AndroidManifest.xml),
         not against this source file: a source file cannot tell you what a
         dependency added. Phase 3 Plan 3 ran that inspection and read the
         merger's blame report — the complete set is RECORD_AUDIO (this file),
         INTERNET + ACCESS_NETWORK_STATE (firebase-firestore:26.5.0), and one
         app-scoped signature-level receiver permission from androidx.core:core,
         which the Flutter embedding pulls in and Firebase does not. No
         analytics, advertising-ID, location or account permission.

         RE-VERIFIED 2026-08-10 (Phase 4 plan 05), after file_picker was added:
         the set above is UNCHANGED — the picker contributes no permission. It
         does contribute one non-permission element, a <queries> entry for
         GET_CONTENT with mime type */*, which is why the merged <queries> block
         has two <intent> children where this file has one. Package visibility,
         not access.

         Re-run that inspection whenever a dependency changes, and grep the
         merged output for `<uses-permission` rather than for `android.permission.*`
         — the latter cannot match an app-namespaced permission, which is how the
         androidx.core entry went unnoticed until Phase 3. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:label="englishreflex"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
