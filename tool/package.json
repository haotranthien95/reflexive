{
  "name": "englishreflex-seed-tool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Disposable one-off Firestore seed for the EnglishReflex question bank (D-45). Superseded by Phase 4's in-app JSON importer (IMPORT-01) — delete this directory when that lands.",
  "scripts": {
    "dry-run": "node seed_questions.mjs --dry-run",
    "seed": "node seed_questions.mjs",
    "verify": "node seed_questions.mjs --verify"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
